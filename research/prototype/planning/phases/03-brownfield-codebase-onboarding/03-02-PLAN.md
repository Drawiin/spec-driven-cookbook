---
phase: 03-brownfield-codebase-onboarding
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - mise-en-place/map-codebase/SKILL.md
  - mise-en-place/spec-phase/SKILL.md
  - mise-en-place/spec-phase/test_spec_phase_contract.py
  - mise-en-place/map-codebase/test_map_codebase_contract.py
  - mise-en-place/README.md
autonomous: false
requirements:
  - PROJ-02
  - PROJ-03
must_haves:
  truths:
    - "Developer can invoke map-codebase workflow and receive seven complete .planning/codebase/*.md files (all 7 required — partial maps fail with retry prompt)"
    - "map-codebase step 6 runs scan_map_secrets.py (fail-closed) before proceeding — not grep-only honor system"
    - "Running /spec-phase on a brownfield repo triggers Step 1.4 detect_brownfield after Step 1 scaffold, before Step 2 Q&A — on every Step 1 branch including Continue with existing SPEC.md"
    - "When has_codebase_map true but fewer than 7 complete files (>20 lines each), Step 1.4 still triggers remap — stub STACK alone insufficient (Addresses review: has_codebase_map vs completeness)"
    - "When Continue with existing brownfield SPEC but map is stale (codebase_map_commit != HEAD) or partial (<7 complete files), re-run mapping before Q&A (Addresses review: Continue policy)"
    - "When needs_codebase_map is true (including empty scaffolded .planning/codebase/), spec-phase runs map-codebase workflow before Q&A"
    - "When detect_brownfield stdout is invalid JSON, spec-phase exits with ERROR and does not proceed to Q&A"
    - "Brownfield Q&A pre-loads one-line summaries from all seven map files and skips re-asking mapped stack/structure facts"
    - "Brownfield mode flag and project_type: brownfield intent set during Step 1.4 (when is_brownfield true), not deferred to Step 4 assembly"
    - "User must confirm/correct map summary before Q&A on brownfield repos — mandatory gate, not skippable under autonomous"
    - "Brownfield SPEC.md assembly includes ## Already Built and ## To Build with project_type: brownfield in frontmatter; validate_spec dedup gate catches identical bullets"
    - "Greenfield repos skip Step 1.4 mapping and produce standard five-section SPEC.md unchanged"
    - "Contract tests parse SKILL.md files and assert step ordering, seven-filename presence, and required branches — not grep-only (Addresses review HIGH: Wave 2 verification)"
  artifacts:
    - path: "mise-en-place/map-codebase/SKILL.md"
      provides: "Parallel mapper workflow writing seven codebase map files with scan_map_secrets gate and embedded template outlines"
      contains: "STACK.md"
    - path: "mise-en-place/spec-phase/SKILL.md"
      provides: "Step 1.4 brownfield branch + brownfield Q&A + assembly rules + stub STACK remap policy"
      contains: "Step 1.4"
    - path: "mise-en-place/spec-phase/test_spec_phase_contract.py"
      provides: "Automated contract tests for spec-phase SKILL step ordering and required branches"
      contains: "test_step_1_4_after_scaffold_before_qa"
    - path: "mise-en-place/map-codebase/test_map_codebase_contract.py"
      provides: "Automated contract tests for map-codebase seven-file output and secret scan invocation"
      contains: "test_all_seven_filenames_documented"
  key_links:
    - from: "spec-phase/SKILL.md Step 1.4"
      to: "detect_brownfield.py"
      via: "Shell: python3 mise-en-place/tool-detect-brownfield/detect_brownfield.py"
      pattern: "tool-detect-brownfield"
    - from: "spec-phase/SKILL.md Step 1.4"
      to: "map-codebase/SKILL.md"
      via: "needs_codebase_map true OR incomplete map (<7 files >20 lines) → execute map-codebase"
      pattern: "map-codebase"
    - from: "map-codebase/SKILL.md step 6"
      to: "scan_map_secrets.py"
      via: "python3 mise-en-place/tool-scan-map-secrets/scan_map_secrets.py --dir .planning/codebase/"
      pattern: "scan_map_secrets"
    - from: "spec-phase/SKILL.md Step 4"
      to: "validate_spec.py BROWNFIELD_SECTIONS + dedup"
      via: "assembly headers ## Already Built and ## To Build match validator; dedup enforced at approval"
      pattern: "## Already Built"
    - from: "map-codebase/SKILL.md"
      to: ".planning/codebase/*.md"
      via: "Task generalPurpose sub-agents Write tool"
      pattern: ".planning/codebase/"
---

## Phase Goal

**As a** developer, **I want to** run `/spec-phase` on an existing codebase and get a spec that separates what's already built from what's new, **so that** I don't re-plan or re-ask about capabilities that already exist.

<objective>
Deliver the brownfield agent workflow vertical slice: `map-codebase/SKILL.md` (standalone + spec-phase-integrated), extended `spec-phase/SKILL.md` with Step 1.4 detection → mapping → delta-focused Q&A → SPEC assembly with Already Built / To Build sections, plus pytest contract tests that parse SKILL.md structure (Addresses review HIGH: replace grep-only Wave 2 verification).

Purpose: PROJ-02 requires codebase mapping before spec Q&A when code exists; PROJ-03 requires the map to seed spec context and enforce separation in SPEC.md. Depends on Plan 03-01 detection, validate_spec dedup, and scan_map_secrets contracts.

Output: `map-codebase/SKILL.md`, extended `spec-phase/SKILL.md`, `test_spec_phase_contract.py`, `test_map_codebase_contract.py`, updated `README.md`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-brownfield-codebase-onboarding/03-RESEARCH.md
@.planning/phases/03-brownfield-codebase-onboarding/03-PATTERNS.md
@.planning/phases/02-spec-phase-greenfield/02-CONTEXT.md
@.planning/phases/03-brownfield-codebase-onboarding/03-01-SUMMARY.md
@.planning/phases/03-brownfield-codebase-onboarding/03-REVIEWS.md

<interfaces>
From Plan 03-01 (must exist before this plan runs):
- detect_brownfield.py JSON: is_brownfield, needs_codebase_map, has_codebase_map, code_extensions_found
- has_codebase_map = STACK.md file exists ONLY — complete map = all 7 files >20 lines (documented in tool-detect-brownfield/SKILL.md)
- validate_spec.py BROWNFIELD_SECTIONS: "## Already Built", "## To Build" (when project_type: brownfield)
- validate_spec inverse rule: brownfield sections in body require project_type: brownfield
- validate_spec dedup rule: identical bullets in both brownfield sections → exit 1
- scan_map_secrets.py: python3 mise-en-place/tool-scan-map-secrets/scan_map_secrets.py --dir .planning/codebase/ — exit 0 clean, exit 1 on secret patterns

From mise-en-place/spec-phase/SKILL.md (extend, preserve Steps 1–5 greenfield flow):
- Step 1: planning_scaffold.py scaffold + mkdir .planning/research; branches for Start fresh vs Continue with existing SPEC.md
- Step 1.4: Brownfield Detection (runs after scaffold, before Step 2 Q&A) — runs on EVERY Step 1 branch
- Step 2: five-category Q&A spine (Problem → Who it's for → Constraints → Success Criteria → Out-of-scope)
- Step 4: REQUIRED_SECTIONS headers must match validate_spec.py exactly
- Sub-agent convention: Task(subagent_type="generalPurpose"), no model parameter

From .cursor/skills/gsd-map-codebase/SKILL.md + .cursor/get-shit-done/workflows/map-codebase.md:
- 4 parallel mapper focuses: tech→STACK+INTEGRATIONS, arch→ARCHITECTURE+STRUCTURE, quality→CONVENTIONS+TESTING, concerns→CONCERNS
- check_existing prompt: Refresh / Update / Skip when .planning/codebase/ exists
- scan_for_secrets step: now automated via scan_map_secrets.py (Plan 03-01) with fail-closed exit 1
- Secret deny rules for mappers: never read .env*, credentials.json, *.pem, secrets.* — note existence only

Seven canonical map files: STACK.md, ARCHITECTURE.md, CONVENTIONS.md, STRUCTURE.md, TESTING.md, INTEGRATIONS.md, CONCERNS.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Slice 1: map-codebase SKILL — standalone parallel mapper workflow with scan_map_secrets gate</name>
  <files>mise-en-place/map-codebase/SKILL.md</files>

  <read_first>
    - .cursor/skills/gsd-map-codebase/SKILL.md (orchestration, 4-agent assignment, check_existing)
    - .cursor/get-shit-done/workflows/map-codebase.md (refresh/skip/update flow, scan_for_secrets step lines 342-374)
    - mise-en-place/tool-scan-map-secrets/scan_map_secrets.py (from Plan 03-01 — Addresses review: fail-closed secret gate)
    - .cursor/get-shit-done/bin/lib/security.cjs (path validation patterns — apply to mapper deny rules)
    - mise-en-place/spec-phase/SKILL.md (sub-agent spawning convention lines 24-27, research sub-agent prompt pattern)
    - .cursor/get-shit-done/templates/codebase/*.md (section structure — embed inline outlines below)
    - .planning/codebase/STACK.md (live output format reference)
    - mise-en-place/tool-context-builder/SKILL.md (context_builder invocation to seed mappers)
  </read_first>

  <action>
    Create mise-en-place/map-codebase/SKILL.md as a workflow skill (no Python script):

    Frontmatter: name map-codebase, description for analyzing existing codebase into seven structured documents.

    Copy cursor_skill_adapter block from spec-phase/SKILL.md (use generalPurpose sub-agents, no model param).

    Objective: parallel mapper sub-agents write directly to .planning/codebase/; orchestrator receives confirmations only (not full document contents).

    Process steps:
    1. Run planning_scaffold.py scaffold to ensure .planning/codebase/ exists
    2. check_existing: if .planning/codebase/ has files, present numbered options — (1) Refresh delete and remap (2) Update specific docs (3) Skip use existing — per GSD map-codebase workflow. When user chooses Skip: if codebase_map_commit in existing SPEC frontmatter differs from current `git rev-parse HEAD` (when git available), warn that map may be stale before proceeding
    3. Optionally run context_builder.py and append stdout to sub-agent prompts as raw snapshot — NEVER include secret values from denied paths in previews; if context_builder output references .env or credential paths, redact values to "(redacted — existence only)"
    4. Spawn 4 parallel Task(subagent_type="generalPurpose") mappers:
       - tech focus → writes STACK.md, INTEGRATIONS.md
       - arch focus → writes ARCHITECTURE.md, STRUCTURE.md
       - quality focus → writes CONVENTIONS.md, TESTING.md
       - concerns focus → writes CONCERNS.md
    5. Each sub-agent prompt MUST include: focus area, output file paths, embedded template section outlines (inline — do NOT rely solely on .cursor/get-shit-done/templates/ path). Copy section headers from templates into SKILL.md for all 7 files:
       - STACK.md: ## Languages, ## Runtime, ## Frameworks, ## Key Dependencies, ## Configuration
       - ARCHITECTURE.md: ## Overview, ## Layers, ## Data Flow, ## Key Patterns
       - STRUCTURE.md: ## Directory Layout, ## Entry Points, ## Key Modules
       - CONVENTIONS.md: ## Naming, ## Code Style, ## Error Handling, ## Testing Conventions
       - TESTING.md: ## Test Framework, ## Test Layout, ## Running Tests, ## Coverage
       - INTEGRATIONS.md: ## External Services, ## APIs, ## Data Stores
       - CONCERNS.md: ## Known Issues, ## Tech Debt, ## Security Notes
    6. Security rules (Addresses review HIGH — fail-closed, not prompt-only):
       - Deny read of: .env, .env.*, credentials.json, *.pem, secrets.*, **/secrets/**, id_rsa, id_ed25519
       - Note existence of secret files only — never read or echo secret values in mapper output or context_builder previews
       - After all 7 files written, run automated secret gate: python3 mise-en-place/tool-scan-map-secrets/scan_map_secrets.py --dir .planning/codebase/
       - If exit 1: STOP — print stderr output, require user to remove/redact flagged content before proceeding; do NOT commit or continue to spec-phase Q&A
       - Document that grep-only manual review is insufficient — scan_map_secrets exit code is the gate
    7. Completeness check: verify ALL 7 files exist AND each has >20 lines. If fewer than 7 files or any file under 20 lines: FAIL with retry prompt — do NOT proceed to spec-phase Q&A with partial/stub map. Stub STACK.md alone must NOT satisfy completeness even when detect_brownfield has_codebase_map true

    Optional --fast mode (research discretion): document single-agent writes-all-7 for repos with fewer than 5 source files; default remains 4-agent parallel.

    Skill invokable standalone via /map-codebase OR map-codebase mention — not only from spec-phase.
  </action>

  <acceptance_criteria>
    - test -f mise-en-place/map-codebase/SKILL.md
    - grep -c "STACK.md" mise-en-place/map-codebase/SKILL.md returns >= 1
    - grep -c "generalPurpose" mise-en-place/map-codebase/SKILL.md returns >= 1
    - grep -c "Refresh" mise-en-place/map-codebase/SKILL.md returns >= 1
    - grep -c "scan_map_secrets" mise-en-place/map-codebase/SKILL.md returns >= 1
    - grep -c ">20 lines" mise-en-place/map-codebase/SKILL.md returns >= 1 OR grep -c "20 lines" mise-en-place/map-codebase/SKILL.md returns >= 1
    - SKILL.md documents all seven output filenames explicitly with embedded section headers
  </acceptance_criteria>

  <verify>
    <automated>test -f mise-en-place/map-codebase/SKILL.md && grep -v '^#' mise-en-place/map-codebase/SKILL.md | grep -c scan_map_secrets | test "$(grep -v '^#' mise-en-place/map-codebase/SKILL.md | grep -c scan_map_secrets)" -ge 1</automated>
  </verify>

  <done>map-codebase/SKILL.md defines complete standalone mapper workflow with scan_map_secrets fail-closed gate, completeness gate, embedded templates, and seven-file output contract</done>
</task>

<task type="auto">
  <name>Slice 2: spec-phase Step 1.4 — detect, completeness-aware mapping, Continue policies</name>
  <files>mise-en-place/spec-phase/SKILL.md</files>

  <read_first>
    - mise-en-place/spec-phase/SKILL.md (full file — insert Step 1.4 at END of Step 1 without breaking Steps 1-5)
    - mise-en-place/tool-detect-brownfield/SKILL.md (has_codebase_map ≠ complete map distinction from Plan 03-01)
    - mise-en-place/map-codebase/SKILL.md (from prior task)
    - .planning/phases/03-brownfield-codebase-onboarding/03-RESEARCH.md (Pattern 4 Step 1.4 brownfield branch)
    - .planning/phases/03-brownfield-codebase-onboarding/03-REVIEWS.md (stub STACK remap, Continue brownfield SPEC policy)
  </read_first>

  <action>
    Extend spec-phase/SKILL.md per RESEARCH Pattern 4 and user_constraints (extend Phase 2, do not replace):

    1. Update objective opening line from "empty repo" to "empty or existing repo" — brownfield supported.

    2. Add **Step 1.4: Brownfield Detection (runs after scaffold, before Step 2 Q&A)** as a sub-step at the END of existing Step 1 (after scaffold + mkdir .planning/research). Do NOT insert anything before Step 1 scaffold — detection runs only after scaffold creates paths.

       **Step 1 branch coverage (mandatory):** Step 1.4 runs on EVERY Step 1 branch before Step 2 opens:
       - Start fresh (no existing SPEC.md)
       - Continue with existing SPEC.md (resume/edit path)

       **Continue path policies (Addresses review MEDIUM):**
       - When user chooses Continue on a brownfield repo with **greenfield-format** SPEC (no project_type: brownfield, no ## Already Built / ## To Build): re-run Step 1.4 detection and mapping before Q&A — do NOT skip to Step 2 with stale greenfield spec on brownfield repo
       - When user chooses Continue on a brownfield repo with **existing brownfield-format** SPEC (project_type: brownfield present): still run Step 1.4 detection; if map is stale (codebase_map_commit in SPEC frontmatter != current git HEAD when git available) OR map is partial (fewer than 7 canonical files each >20 lines): re-run map-codebase workflow before Q&A — do NOT proceed with stale/partial map even when user chose Continue

       Step 1.4 procedure:
       a. Run: python3 mise-en-place/tool-detect-brownfield/detect_brownfield.py
       b. Parse JSON from stdout — if stdout is not valid JSON: print ERROR: detect_brownfield returned invalid JSON to stderr; STOP — do not proceed to Step 2 Q&A
       c. If is_brownfield false → skip to Step 2 greenfield Q&A (no mapping)
       d. If is_brownfield true:
          - Set internal brownfield mode flag AND project_type intent to brownfield immediately (not deferred to Step 4)
          - **Completeness check (Addresses review MEDIUM-HIGH):** Count canonical map files at .planning/codebase/ (STACK.md, ARCHITECTURE.md, CONVENTIONS.md, STRUCTURE.md, TESTING.md, INTEGRATIONS.md, CONCERNS.md). Map is complete only when ALL 7 exist AND each has >20 lines. If needs_codebase_map true OR map incomplete (including stub STACK.md with has_codebase_map true from detect JSON): execute map-codebase workflow (read map-codebase/SKILL.md). Do NOT trust has_codebase_map JSON alone — stub STACK satisfies detection but NOT completeness
          - If has_codebase_map true AND needs_codebase_map false AND map complete (all 7 files >20 lines) → offer check_existing Refresh/Update/Skip per map-codebase SKILL; on Skip verify codebase_map_commit matches HEAD or warn stale
          - Defense-in-depth: after any mapping attempt, re-verify completeness; if still incomplete, fail with retry prompt per map-codebase — do NOT proceed to Q&A with partial/stub map
          - Read one-line summaries from ALL seven map files: STACK.md, ARCHITECTURE.md, STRUCTURE.md, CONVENTIONS.md, TESTING.md, INTEGRATIONS.md, CONCERNS.md (summaries only — do NOT dump full file contents)
          - Present 3-5 bullet summary of detected capabilities; **require** user to confirm/correct before Q&A — mandatory human gate, not optional under autonomous execution
          - Continue to Step 2 with brownfield mode active

    3. Document that Step 1.4 preserves clean-context constraint (SPEC-05): all inputs from local files and tool stdout only.

    Label consistently: "Step 1.4: Brownfield Detection (runs after scaffold, before Step 2 Q&A)" — no "Step 0" language anywhere in spec-phase/SKILL.md.
  </action>

  <acceptance_criteria>
    - grep -c "Step 1.4" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -v '^#' mise-en-place/spec-phase/SKILL.md | grep -c "Step 0" returns 0
    - grep -c "Continue" mise-en-place/spec-phase/SKILL.md returns >= 1 (Step 1 branch coverage documented)
    - grep -c "brownfield-format" mise-en-place/spec-phase/SKILL.md returns >= 1 OR grep -c "existing brownfield" mise-en-place/spec-phase/SKILL.md returns >= 1 (Continue brownfield SPEC policy)
    - grep -c "has_codebase_map" mise-en-place/spec-phase/SKILL.md returns >= 1 AND grep -c "complete" mise-en-place/spec-phase/SKILL.md returns >= 1 (stub STACK remap policy)
    - grep -c "invalid JSON" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "confirm" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "CONVENTIONS.md" mise-en-place/spec-phase/SKILL.md returns >= 1 (all-seven preload)
    - grep -c "tool-detect-brownfield" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "map-codebase" mise-en-place/spec-phase/SKILL.md returns >= 1
    - Greenfield path documented: is_brownfield false skips to Step 2 without mapping
  </acceptance_criteria>

  <verify>
    <automated>grep -v '^#' mise-en-place/spec-phase/SKILL.md | grep -cE 'Step 1\.4|has_codebase_map|complete|Continue|invalid JSON' | test "$(grep -v '^#' mise-en-place/spec-phase/SKILL.md | grep -cE 'Step 1\.4|has_codebase_map|complete|Continue|invalid JSON')" -ge 4</automated>
  </verify>

  <done>spec-phase Step 1.4 triggers detection and completeness-aware mapping before Q&A on all Step 1 branches; stub STACK + has_codebase_map true still remaps when incomplete; Continue brownfield SPEC re-maps on stale/partial map; greenfield path unchanged</done>
</task>

<task type="auto" tdd="true">
  <name>Slice 3: Brownfield Q&A, SPEC assembly, contract tests, and README</name>
  <files>mise-en-place/spec-phase/SKILL.md, mise-en-place/spec-phase/test_spec_phase_contract.py, mise-en-place/map-codebase/test_map_codebase_contract.py, mise-en-place/README.md</files>

  <read_first>
    - mise-en-place/spec-phase/SKILL.md (Step 2 Q&A and Step 4 assembly sections — after Slice 2 edits)
    - mise-en-place/map-codebase/SKILL.md (after Slice 1)
    - mise-en-place/tool-validate-spec/validate_spec.py (REQUIRED_SECTIONS + BROWNFIELD_SECTIONS + dedup from Plan 03-01)
    - .planning/phases/03-brownfield-codebase-onboarding/03-RESEARCH.md (Pattern 3 SPEC format, confidence tags, Q&A delta focus)
    - .planning/phases/03-brownfield-codebase-onboarding/03-REVIEWS.md (contract test requirement — Addresses review HIGH)
    - mise-en-place/tool-validate-spec/test_validate_spec.py (pytest pattern for contract tests — stdlib pathlib + re only)
  </read_first>

  <behavior>
    Contract tests (Addresses review HIGH — Wave 2 automated verification, not grep-only):

    test_spec_phase_contract.py:
    - test_step_1_4_after_scaffold_before_qa: parse SKILL.md; assert "Step 1.4" appears after "Step 1" and before "Step 2" (position ordering via str.find)
    - test_no_step_0_language: non-comment lines must not contain "Step 0"
    - test_all_seven_map_filenames_preloaded: assert all seven canonical filenames (STACK.md, ARCHITECTURE.md, CONVENTIONS.md, STRUCTURE.md, TESTING.md, INTEGRATIONS.md, CONCERNS.md) appear in spec-phase/SKILL.md
    - test_detect_brownfield_invocation_documented: assert "tool-detect-brownfield" or "detect_brownfield.py" in SKILL.md
    - test_invalid_json_stops_workflow: assert "invalid JSON" in SKILL.md body
    - test_continue_branch_documented: assert "Continue" in SKILL.md
    - test_completeness_gate_documented: assert "20 lines" or ">20 lines" in SKILL.md AND "has_codebase_map" in SKILL.md (stub STACK policy)
    - test_brownfield_sections_in_assembly: assert "## Already Built" and "## To Build" in SKILL.md

    test_map_codebase_contract.py:
    - test_all_seven_filenames_documented: assert all seven canonical map filenames in map-codebase/SKILL.md
    - test_scan_map_secrets_invocation: assert "scan_map_secrets" in map-codebase/SKILL.md
    - test_completeness_gate_documented: assert "20 lines" or ">20 lines" in map-codebase/SKILL.md
    - test_parallel_mapper_focuses: assert "generalPurpose" in map-codebase/SKILL.md
  </behavior>

  <action>
    Extend spec-phase/SKILL.md Step 2 for brownfield mode (when Step 1.4 set brownfield flag):
    - Same five-category spine order preserved (Phase 2 D-01)
    - Do NOT re-ask stack, directory structure, conventions, testing approach, integrations, or concerns already documented in codebase map summaries (all seven files preloaded in Step 1.4)
    - Problem and Success Criteria focus on DELTA — new work only, not inventory of existing code
    - Success Criteria must reference To Build items only in brownfield mode (no mixing built capabilities)
    - Confidence tags on Already Built items during Q&A confirmation: [observed] for direct code evidence, [inferred] for pattern-based, [unverified] for uncertain (research discretion)

    Extend spec-phase/SKILL.md Step 4 assembly for brownfield:
    - Frontmatter adds project_type: brownfield (already set as intent in Step 1.4 — Step 4 writes it to SPEC.md); greenfield when not brownfield — default greenfield
    - Optional codebase_map_commit: git rev-parse HEAD when git available (skip gracefully if not repo)
    - Body order for brownfield: ## Already Built (from map + user confirmation), ## To Build (active requirements), then existing five REQUIRED_SECTIONS
    - Assembly rule: every To Build item must be a gap not covered in Already Built — no duplication across sections (RESEARCH Pitfall 3); validate_spec dedup gate will reject identical bullets at Step 5 approval
    - Header strings must match validate_spec.py exactly including ## Already Built and ## To Build

    Step 5 approval gate unchanged — validate_spec handles brownfield sections, inverse rule, and dedup when approved.

    Write mise-en-place/spec-phase/test_spec_phase_contract.py and mise-en-place/map-codebase/test_map_codebase_contract.py per behavior block. Use pathlib.Path.read_text() and plain assert/re — no external deps. Tests read SKILL.md files directly; they validate workflow contract structure, not agent runtime behavior.

    Update mise-en-place/README.md:
    - Add ### map-codebase section describing seven-file output, scan_map_secrets fail-closed gate, completeness gate, and standalone invocation
    - Update ### spec-phase description to mention Step 1.4 brownfield detection, completeness-aware mapping, mandatory confirm gate, Continue policies, and Already Built / To Build sections
    - Cross-reference tool-detect-brownfield and tool-scan-map-secrets from Plan 03-01
    - Note contract tests: pytest mise-en-place/spec-phase/test_spec_phase_contract.py mise-en-place/map-codebase/test_map_codebase_contract.py
  </action>

  <acceptance_criteria>
    - grep -c "## Already Built" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "## To Build" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "project_type" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "dedup" mise-en-place/spec-phase/SKILL.md returns >= 1 OR grep -c "duplicate" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "map-codebase" mise-en-place/README.md returns >= 1
    - grep -c "brownfield" mise-en-place/README.md returns >= 1
    - python3 -m pytest mise-en-place/spec-phase/test_spec_phase_contract.py mise-en-place/map-codebase/test_map_codebase_contract.py -x -q exits 0
    - python3 -m pytest mise-en-place/ -q exits 0 (no test regressions)
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest mise-en-place/spec-phase/test_spec_phase_contract.py mise-en-place/map-codebase/test_map_codebase_contract.py mise-en-place/ -x -q</automated>
  </verify>

  <done>Brownfield Q&A skips mapped facts from all seven files; SPEC assembly produces separated Already Built / To Build sections with dedup enforced at validate_spec; contract tests prove SKILL step ordering and required branches; README documents full brownfield workflow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mapper sub-agents → filesystem | Agents read project source and write .planning/codebase/ |
| spec-phase → user Q&A | Conversational flow loads map summaries into agent context |
| context_builder → mapper prompts | Raw snapshot may reference sensitive paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | map-codebase mapper agents | mitigate | Deny read of .env*, credentials.json, *.pem, secrets.*; scan_map_secrets.py fail-closed exit 1; never include secret values in mapper output or context_builder previews |
| T-03-05 | Tampering | Stale codebase map | mitigate | Warn when codebase_map_commit != HEAD on Skip; Continue brownfield SPEC re-maps on stale/partial; completeness gate requires all 7 files >20 lines |
| T-03-06 | Spoofing | Inferred capabilities as requirements | mitigate | Mandatory user confirmation gate after map before Q&A; [inferred]/[unverified] tags |
| T-03-07 | Tampering | Partial/stub map accepted | mitigate | Step 1.4 completeness check overrides has_codebase_map JSON; fail with retry prompt if fewer than 7 files or any file under 20 lines |
| T-03-SC | Tampering | npm/pip installs | accept | No package installs in this plan — SKILL.md workflow only |

</threat_model>

<verification>
Automated: contract tests + full pytest suite green after README edits.

Manual UAT per 03-VALIDATION.md — **BLOCKING for /gsd-verify-work** (phase verify cannot pass until manual UAT checklist complete):
- Run /map-codebase on this repo → 7 complete files in .planning/codebase/; scan_map_secrets exits 0
- Run /spec-phase on this repo → Step 1.4 detect → map → confirm gate → Q&A references map, focuses on delta
- Run /spec-phase Continue path on brownfield repo with existing greenfield SPEC → Step 1.4 re-runs detection/mapping before Q&A
- Run /spec-phase Continue path on brownfield repo with existing brownfield SPEC but stale/partial map → re-run mapping before Q&A
- Complete brownfield spec → SPEC.md has distinct Already Built and To Build sections; validate_spec dedup rejects duplicates
- Run /spec-phase on empty tmp dir → no Step 1.4 mapping step, standard five sections
</verification>

<success_criteria>
- map-codebase/SKILL.md invokable standalone with scan_map_secrets fail-closed gate, embedded templates, and 4-focus parallel mapper contract
- spec-phase Step 1.4 integrates detect_brownfield + completeness-aware map-codebase on all Step 1 branches including Continue brownfield SPEC stale/partial remap
- Brownfield SPEC.md assembly matches validate_spec BROWNFIELD_SECTIONS + dedup contract
- Contract tests (test_spec_phase_contract.py, test_map_codebase_contract.py) pass — Wave 2 structure verified without grep-only gates
- Greenfield flow preserved for is_brownfield false repos
- README documents map-codebase, brownfield spec-phase, and contract test commands
</success_criteria>

<output>
Create `.planning/phases/03-brownfield-codebase-onboarding/03-02-SUMMARY.md` when done
</output>
