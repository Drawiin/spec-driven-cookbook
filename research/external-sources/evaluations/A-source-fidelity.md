# Evaluator A — Source Fidelity Report

> Evaluator: A (source-fidelity)
> Date: 2026-05-24
> Inputs: four reviews + four source summaries (immutable) ↔ four target dossier files (mutable)

## 1. Scope checked

- **New patterns checked: 13** — Patterns 20–32 in `topics/patterns-worth-stealing.md`.
- **Extensions checked: 9** — Pattern 4 (go-wrong log), Pattern 5 (vertical slice), Pattern 6 (per-hunk attribution), Pattern 8 (universal diagnose + `causesAnotherCycle`), Pattern 11 (×4: architecture-rule tests / self-describing rules / redundancy axis / per-hunk attribution), Pattern 13 (Markers + bytecode substrate), Pattern 15 (versioned distribution + harness templates), Pattern 16 (versioned constitution with provenance). Pattern 8 carries two distinct extensions (one OpenRewrite, one Memo #13).
- **Other sections checked: 9**
  - `topics/self-healing-and-verification.md` §2 (ScanningRecipe, Scope precondition, Shift-left review sub-sections), §3 (`causesAnotherCycle` paragraph), §8 (5 new bullets: impact-radius, regulation categories, remediation embedding, quality-left distribution, survey-then-edit, shift-left review), §9 (Ergonomic Gates), §10 (Frozen Baselines).
  - `topics/workflow-and-orchestration.md` §8 (3 new bullets: steering, repair-vs-update-harness, Recipe DAG).
  - `SUMMARY.md` §1 (Ashby framing), §4 (outer-harness paragraph), §4 (13 brief tag-lines), §6 (external sources block), §7 (2 new open questions), §9 (incorporation log).
- **Spot-check claims verified: 26** (2 per new pattern × 13 patterns; results in §4).

## 2. Auto-fixes applied

| # | File | Pattern/section | Issue | Fix |
|---|---|---|---|---|

*(No auto-fixes were applied. The two material defects identified — duplicated rows in the Pattern Ranking Summary Table and duplicated §9 headings in `SUMMARY.md` — require content deletion, which is explicitly forbidden by the auto-fix rules. They are recorded in §3 below for human triage.)*

## 3. FOR HUMAN — Issues not auto-fixed

| # | File | Pattern/section | Issue | Why not auto-fixed |
|---|---|---|---|---|
| 1 | `topics/patterns-worth-stealing.md` | Pattern Ranking Summary Table (lines 446–471) | Rows for Patterns 20–32 appear twice. The first set (lines 446–458) and the second set (lines 459–471) cover the same pattern numbers with slightly different transferability annotations and problem statements (e.g., row 20 first reads `HIGH \| Text-level editing limits and hallucinated APIs`, second reads `HIGH (where IR investment is justified) \| Text-level edits hide hallucinated APIs`). Both blocks survive in the file. | Requires deleting 13 rows. Rule "MUST NOT: Delete content." applies. Human must pick which set to keep (recommend the second set — its phrasing tracks each pattern's body more closely, e.g., the Pattern 20 transferability annotation matches the body verbatim). |
| 2 | `SUMMARY.md` | §9 Incorporation Log (lines 313–324 and 328–356) | Two consecutive `## 9. Incorporation Log — 2026-05-24` sections. Both describe the same incorporation; the second is more complete (adds the four-stage subagent pipeline narrative, a "Items skipped" list, a "Risks / known issues" list, and a pointer to `INCORPORATION-REPORT.md`). The two tables overlap significantly but are not byte-identical. | Requires deleting one full section. Rule "MUST NOT: Delete content." applies. Human must pick which to keep (recommend the second — more complete and matches the `INCORPORATION-REPORT.md` companion file). |
| 3 | `topics/patterns-worth-stealing.md` | Pattern 21 placement vs Pattern 20 | Patterns appear non-monotonically: Pattern 21 (Authored Composition / Recipe DAG) sits in Group B between Pattern 9 and Pattern 10, while Pattern 20 (LST substrate) sits in Group C between Pattern 14 and Pattern 22. This matches the reviewer's "Proposed home" guidance (Recipe DAG → Group B Workflow; LST → Group C Verification), so the placement is intentional, but the resulting non-sequential numbering can confuse a reader scanning for "Pattern 20" after "Pattern 19". | Not a fidelity defect against the reviewer proposals; flagged only so a human can decide whether to add a one-line forward-reference at the end of Group B/Group A pointing readers to the cross-group jumps. Restructuring or renumbering is explicitly forbidden. |
| 4 | `topics/self-healing-and-verification.md` | §2 "Survey-then-edit" content vs §8 "Survey-then-edit" content | OpenRewrite's `ScanningRecipe` claim now appears in two places: §2 ("ScanningRecipe accumulator (OpenRewrite extension)" sub-section, lines 45–46) and §8 ("Survey-then-edit with a typed accumulator" bullet, line 223). Wording differs but substance overlaps. The reviewer's EXTENDS proposal targeted §2 only; the §8 bullet appears to be an incorporator addition. | Both are faithful to the source, so this is not a fidelity violation. Flagged so a human can decide whether to keep both (defensible — §8 reads as the cross-cutting implication) or consolidate. Deduplicating would require deleting one of the two passages. |
| 5 | `topics/patterns-worth-stealing.md` | Pattern 11 — "Extension — generalizes to architecture-rule tests" | Reviewer's proposed sub-bullet ended at "the abstraction transfers to any AST/IR layer." Dossier text appends "(e.g., dependency-cruiser for JavaScript, ts-arch for TypeScript)" — concrete examples not in the source summary. ArchUnit summary covers Java only; the JS/TS analogs are incorporator additions, not in the source. | Minor enrichment that goes slightly beyond what the source supports. Reasonable additions but technically unverified against the source summary. Recommend either citing a source for dependency-cruiser/ts-arch or softening to "(JS and TS have analogous tools)". Below the threshold for the listed auto-fix categories. |
| 6 | `topics/patterns-worth-stealing.md` | Pattern 32 origin attribution: "Inferred from Böckeler Memo #13 examples ...; Graphify's `query_graph` / `get_neighbors` is the natural mechanism." | The Memo #13 source explicitly proposes "Reuse-awareness pre-check" as a mechanism (`### Mechanisms / practices worth stealing` row 5: "Reuse-awareness pre-check \| scan codebase for existing components before generating new ones"). It is therefore not strictly "inferred from examples"; the memo names the mechanism by name. The "[inferred]" framing is from the reviewer (Item N3) and is faithful to the reviewer text, but slightly understates how explicit the source is. | Marginal — the dossier faithfully reflects the reviewer's classification. Could be tightened to "Origin: Böckeler Memo #13 §6 (mechanisms worth stealing); Graphify is the natural implementation mechanism." Below the threshold for the listed auto-fix categories. |

## 4. Spot-check results (2 random claims per new pattern)

| Pattern | Claim | Source summary anchor | Verdict |
|---|---|---|---|
| 20 (LST) | "Type information travels with each node, so the model has grounding against fabricated APIs" | `01-openrewrite.md` §2 "Type-attributed. Each LST is imbued with type information..." | TRACED |
| 20 (LST) | "whitespace and local style are tree-resident, so unrelated lines are never reformatted" | `01-openrewrite.md` §2 "Format-preserving. Whitespace before and after LSTs are preserved..." | TRACED |
| 21 (Recipe DAG) | "imperative (code) and declarative YAML that composes other recipes via `recipeList`" | `01-openrewrite.md` §2 "Two flavors: imperative (Java class extending `Recipe`...) and declarative (YAML that composes other recipes via `recipeList`)" | TRACED |
| 21 (Recipe DAG) | "The composition surface is the unit of reuse; the planner does tool-selection over the DAG, not over chat prompts" | `01-openrewrite.md` §5 inference row (adaptation suggestion); reviewer N1 | TRACED (adaptation, transparently flagged as such by `[inference]` markers in the summary) |
| 22 (Precondition) | "Preconditions ... gate which files a skill may touch *before* the LLM is invoked" | `01-openrewrite.md` §4 "AI agents need cheap, deterministic gating ... before invoking expensive model calls"; §2 "lightweight search recipes that gate whether a given recipe is allowed to edit a file" | TRACED |
| 22 (Precondition) | "glob, AST query, type predicate, or short search recipe" | `01-openrewrite.md` §2 names only "search recipes"; reviewer expanded to glob/AST/type list. Reasonable extrapolation of the precondition mechanism. | PARTIAL — the precondition primitive is sourced; the specific list of forms is the reviewer's elaboration. |
| 23 (Iso vs Non-Iso) | "isomorphic (provably structure-preserving) and non-isomorphic (allows node-type replacement) edits" | `01-openrewrite.md` §2 "isomorphic (`JavaIsoVisitor`, type-preserving) and non-isomorphic (`JavaVisitor`, allows swapping node types)" | TRACED |
| 23 (Iso vs Non-Iso) | "Apply different review gates per class — safe edits can auto-merge after gate checks; structural edits require explicit human or higher-tier-model approval." | `01-openrewrite.md` §5 adaptation row "Different review gates per class" | TRACED (adaptation, transparently flagged) |
| 24 (Manifest) | "OpenRewrite recipe descriptors (`@Option`, displayName, `estimatedEffortPerOccurrence`, tags)" | `01-openrewrite.md` §5 "Recipes self-document their parameters and expected effort saved" + explicit name `estimatedEffortPerOccurrence` | TRACED |
| 24 (Manifest) | "the planner LLM does tool-selection over (no prompt-time prose)" | `01-openrewrite.md` §4 "an AI orchestrator can plan over a discoverable, typed catalog of capabilities" | TRACED (adaptation) |
| 25 (Frozen Baseline) | "`FreezingArchRule.freeze(rule)` snapshots every existing violation into a VCS-committed `ViolationStore` text file; CI fails only on *new* violations" | `02-archunit.md` §3 + §5 "`FreezingArchRule` records every existing violation in a VCS-committed `ViolationStore`; CI fails only on *new* violations and auto-shrinks the store" | TRACED |
| 25 (Frozen Baseline) | "Two CI-side safety flags (`freeze.store.default.allowStoreCreation=false`, `allowStoreUpdate=false`) prevent silent baseline growth" | `02-archunit.md` §3 "Configure CI safety via `freeze.store.default.allowStoreCreation=false` and `allowStoreUpdate=false` so CI can never silently grow the baseline" | TRACED |
| 26 (Diagram-as-Spec) | "PlantUML component diagram with stereotyped components (`<<..pkg..>>`)" | `02-archunit.md` §5 "A PlantUML component diagram with stereotyped components (`<<..pkg..>>`) becomes an executable rule" | TRACED |
| 26 (Diagram-as-Spec) | "any code-level edge not present in the diagram fails the build" | `02-archunit.md` §5 "Components are mapped to package identifiers; arrows are the only allowed dependencies"; §6 quote "ArchUnit can derive rules straight from PlantUML diagrams and check to make sure that all imported `JavaClasses` abide by the dependencies of the diagram" | TRACED |
| 27 (Importable Spec Packs) | "`ArchTests.in(SharedRules.class)` plus `@AnalyzeClasses(packages=...)` lets an organization publish a rule library" | `02-archunit.md` §2 "`ArchTests.in(ServiceRules.class)` composes rule libraries"; §5 "Rules can be packaged in classes, imported across modules" | TRACED |
| 27 (Importable Spec Packs) | "Analog to ESLint shared configs or shared lint rule sets, but with semantic, cross-file architectural scope" | `02-archunit.md` §5 "Analog to lint-rule packages but with semantic, cross-file scope" | TRACED |
| 28 (Named Primitives) | "`layeredArchitecture().layer(..).definedBy(..).whereLayer(..).mayOnlyBeAccessedByLayers(..)`" | `02-archunit.md` §2 same fluent chain | TRACED |
| 28 (Named Primitives) | "`onionArchitecture().domainModels(..).domainServices(..).applicationServices(..).adapter(..)`" | `02-archunit.md` §2 same fluent chain | TRACED |
| 29 (Guides + Sensors) | "guide (feedforward — anticipates misbehaviour and steers before action, e.g., AGENTS.md, skills, code-mod recipes, LSP access)" | `03-harness-engineering.md` §4 same enumeration | TRACED |
| 29 (Guides + Sensors) | "Either alone is insufficient: guides without sensors cannot be verified; sensors without guides surface failures the agent has no instruction to avoid" | `03-harness-engineering.md` §3 "Two control directions: Guides and Sensors. Either alone is insufficient" | TRACED |
| 30 (Computational vs Inferential) | "computational (deterministic — linters, type checkers, fitness functions, structural/architectural tests, mutation tests)" | `03-harness-engineering.md` §3 same set | TRACED |
| 30 (Computational vs Inferential) | "inferential (LLM-based — AI review, LLM-as-judge, semantic diff)" | `03-harness-engineering.md` §3 + §6 mechanism table | TRACED |
| 31 (Approved Scenarios) | "Behaviour tests are stored as domain-friendly fixture files combining an input and its expected output" | `03-harness-engineering.md` §4 "Approved fixtures pattern ... domain-specific approval files combining input + expected output" | TRACED |
| 31 (Approved Scenarios) | "The runner regenerates the expected-output side on demand; the reviewer's job is a diff review, not assertion authorship" | `03-harness-engineering.md` §6 "runner regenerates, reviewer diffs" | TRACED |
| 32 (Reuse Pre-Check) | "Generative agents re-implement existing components instead of importing them" | `04-role-of-developer-skills.md` §4 "Modularity / reuse awareness ... AI re-implements a UI component that already exists" | TRACED |
| 32 (Reuse Pre-Check) | "a pre-step queries a codebase index (Graphify graph, ripgrep, ast-grep) for symbols semantically close to the requested API surface" | `04-role-of-developer-skills.md` §6 "scan codebase for existing components before generating new ones; inject a 'have we already built this?' search step". Graphify-specific mechanism is the reviewer's mapping, not the memo. | PARTIAL — the pre-check mechanism is sourced; the specific tools (Graphify, ripgrep, ast-grep) are the reviewer's elaboration. |

## 5. Overall fidelity verdict

**HIGH.** Across 13 new patterns, 9 extension sub-bullets, and ~17 cross-cutting paragraphs in the three topic files and `SUMMARY.md`, every load-bearing claim spot-checked traces back to the relevant source summary, every reviewer-proposed URL is preserved in the dossier text, every origin source (OpenRewrite / ArchUnit / Harness Engineering / Böckeler Memo #13) is named in the corresponding pattern body or extension, and the few cases of incorporator elaboration beyond the source (Pattern 11 JS/TS analog examples, Pattern 22 list of precondition forms, Pattern 32 tool list) are reasonable extrapolations rather than fabrications. Cross-references between new and existing patterns (Pattern 8 ↔ Pattern 29; Pattern 11 extension ↔ Pattern 13; Pattern 28 ↔ §8 regulation-categories bullet; Pattern 31 ↔ Pattern 11) are internally consistent and point to patterns that actually exist with the cited numbers. The two material defects are *structural* (duplicated Pattern Ranking Summary Table rows; duplicated `SUMMARY.md` §9) rather than fidelity issues — they preserve all source claims while introducing redundancy. Both are flagged in §3 for human triage because the strict auto-fix rules forbid content deletion; both are mechanical fixes for a human in under five minutes. No semantic divergence, no fabricated source claim, and no missing URL was detected.
