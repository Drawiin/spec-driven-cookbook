---
phase: 02-spec-phase-greenfield
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mise-en-place/tool-validate-spec/validate_spec.py
  - mise-en-place/tool-validate-spec/test_validate_spec.py
autonomous: true
requirements:
  - SPEC-02
  - SPEC-04

must_haves:
  truths:
    - "python3 mise-en-place/tool-validate-spec/validate_spec.py exits 0 when .planning/SPEC.md has status: approved and all five required section headers"
    - "python3 mise-en-place/tool-validate-spec/validate_spec.py exits 1 with actionable stderr when status is draft"
    - "python3 mise-en-place/tool-validate-spec/validate_spec.py exits 1 with actionable stderr when SPEC.md is absent"
    - "python3 mise-en-place/tool-validate-spec/validate_spec.py exits 1 with actionable stderr when front-matter is malformed"
    - "python3 mise-en-place/tool-validate-spec/validate_spec.py exits 1 with actionable stderr when status is approved but a required section header is missing"
    - "python3 -m pytest mise-en-place/tool-validate-spec/ -x -q exits 0 (all 6 test functions pass)"
  artifacts:
    - path: "mise-en-place/tool-validate-spec/validate_spec.py"
      provides: "Stdlib-only SPEC.md approval validator with status + section-header validation"
      contains: "REQUIRED_SECTIONS"
    - path: "mise-en-place/tool-validate-spec/test_validate_spec.py"
      provides: "pytest coverage for all six exit-code behaviors"
      contains: "def test_approved_spec_exits_zero"
  key_links:
    - from: "validate_spec.py:main()"
      to: ".planning/SPEC.md"
      via: "SPEC_CANDIDATES list, pathlib.Path.exists()"
      pattern: "SPEC_CANDIDATES"
    - from: "validate_spec.py:main()"
      to: "SPEC.md body sections"
      via: "REQUIRED_SECTIONS header scan after status check"
      pattern: "REQUIRED_SECTIONS"
    - from: "validate_spec.py:parse_frontmatter()"
      to: "SPEC.md front-matter block"
      via: ".splitlines() + line.partition(':')"
      pattern: "def parse_frontmatter"
    - from: "test_validate_spec.py"
      to: "validate_spec.py"
      via: "sys.path.insert(0, ...) then from validate_spec import main"
      pattern: "sys.path.insert"
---

<objective>
Deliver the Python validation gate for SPEC.md approval status and required section structure: the implementation file and its full test suite, created via the TDD RED→GREEN cycle.

Purpose: D-09 belt-and-suspenders enforcement — plan-phase cannot proceed unless `validate_spec.py` exits 0. Tests written first ensure the exit-code contract and SPEC-02 section structure are locked before the implementation is authored.

Output: `mise-en-place/tool-validate-spec/validate_spec.py` (implementation) + `mise-en-place/tool-validate-spec/test_validate_spec.py` (6 pytest functions covering SPEC-04 status behaviors and SPEC-02 section-header enforcement).

Addresses review concern: HIGH test/implementation mismatch — GREEN task implements `REQUIRED_SECTIONS` body scan matching `test_approved_spec_has_required_sections`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-spec-phase-greenfield/02-CONTEXT.md
@.planning/phases/02-spec-phase-greenfield/02-RESEARCH.md
@.planning/phases/02-spec-phase-greenfield/02-REVIEWS.md
</context>

<feature>
  <name>tool-validate-spec — SPEC.md approval gate (stdlib-only Python)</name>
  <files>mise-en-place/tool-validate-spec/validate_spec.py, mise-en-place/tool-validate-spec/test_validate_spec.py</files>
  <behavior>
    Canonical section headers (case-sensitive, exact match required in body):
    - ## Problem
    - ## Who It's For
    - ## Constraints
    - ## Success Criteria
    - ## Out of Scope

    Behaviors verified by the six test functions:
    - test_approved_spec_exits_zero: .planning/SPEC.md with `status: approved` and all five section headers → sys.exit(0)
    - test_draft_spec_exits_one: .planning/SPEC.md with `status: draft` → sys.exit(1), stderr contains "must be 'approved'"
    - test_missing_spec_exits_one: no SPEC.md anywhere → sys.exit(1), stderr contains "not found"
    - test_malformed_frontmatter_exits_one: SPEC.md without `---` front-matter block → sys.exit(1), stderr contains "no valid YAML front-matter"
    - test_approved_spec_has_required_sections: SPEC.md with status: approved and all five canonical section headers → sys.exit(0)
    - test_approved_spec_missing_section_exits_one: SPEC.md with status: approved but omitting ## Constraints → sys.exit(1), stderr contains "missing required section"

    All six test functions must be RED (fail) before the implementation file exists.
  </behavior>
  <implementation>
    validate_spec.py uses pathlib + sys only (no imports outside stdlib).
    Key exports: SPEC_CANDIDATES list, REQUIRED_SECTIONS list, parse_frontmatter(text: str) → dict, main() → None.
    REQUIRED_SECTIONS = ["## Problem", "## Who It's For", "## Constraints", "## Success Criteria", "## Out of Scope"] — document these exact strings in module docstring.
    parse_frontmatter uses text.splitlines() (CRLF-safe), skips blank and comment lines, uses line.partition(':') to split key/value.
    main() iterates SPEC_CANDIDATES = [Path(".planning/SPEC.md"), Path("SPEC.md")], prints actionable stderr and calls sys.exit(1) for: file not found, missing front-matter, status != "approved", missing required section header. After status == "approved" passes, scan full file text for each header in REQUIRED_SECTIONS; if any absent, print "ERROR: SPEC.md missing required section: {header}" to stderr and exit 1. On success prints confirmation to stdout and calls sys.exit(0).
  </implementation>
</feature>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Write test_validate_spec.py — six failing test functions</name>
  <files>mise-en-place/tool-validate-spec/test_validate_spec.py</files>

  <read_first>
    - mise-en-place/tool-planning-scaffold/test_planning_scaffold.py (pytest fixture pattern: tmp_path, sys.path.insert, SystemExit assertions)
    - mise-en-place/tool-env-check/test_env_check.py (monkeypatch pattern, capsys for stderr capture)
    - .planning/phases/02-spec-phase-greenfield/02-VALIDATION.md (exact test function names and SPEC-04 requirement mapping)
    - .planning/phases/02-spec-phase-greenfield/02-REVIEWS.md (HIGH: section validation must match implementation; MEDIUM: canonical header strings)
  </read_first>

  <action>
    Create mise-en-place/tool-validate-spec/ directory (via first Write call). Write test_validate_spec.py with:

    Imports: sys, pathlib.Path, pytest. Use sys.path.insert(0, str(Path(__file__).parent)) to import validate_spec.

    Define a module-level constant CANONICAL_SECTIONS matching the validator contract (used in fixture bodies):
    ["## Problem", "## Who It's For", "## Constraints", "## Success Criteria", "## Out of Scope"]

    Six test functions — each uses monkeypatch.chdir(tmp_path) to control working directory, creates .planning/ subdir inside tmp_path, writes a SPEC.md fixture there, then calls main() inside pytest.raises(SystemExit) and asserts on exc.value.code:

    1. test_approved_spec_exits_zero: write .planning/SPEC.md with front-matter `status: approved` and body containing all five CANONICAL_SECTIONS headers (each on its own line followed by placeholder content), assert sys.exit code == 0.
    2. test_draft_spec_exits_one: write .planning/SPEC.md with front-matter `status: draft`, assert code == 1. Capture capsys.readouterr().err, assert "must be 'approved'" in stderr.
    3. test_missing_spec_exits_one: do not create any SPEC.md, assert code == 1. Assert "not found" in stderr.
    4. test_malformed_frontmatter_exits_one: write .planning/SPEC.md with content that starts with body text and no `---` block, assert code == 1. Assert "no valid YAML front-matter" in stderr.
    5. test_approved_spec_has_required_sections: write .planning/SPEC.md with status: approved and body containing all five CANONICAL_SECTIONS headers exactly (case-sensitive), assert code == 0. Addresses review concern: SPEC-02 section structure enforced at gate.
    6. test_approved_spec_missing_section_exits_one: write .planning/SPEC.md with status: approved and body containing ## Problem, ## Who It's For, ## Success Criteria, ## Out of Scope but deliberately omitting ## Constraints. Assert code == 1. Capture capsys.readouterr().err, assert "missing required section" in stderr and "## Constraints" in stderr. Addresses review concern: MEDIUM negative section test.

    Import strategy (lazy — enables pytest collection before validate_spec.py exists):
    - At module level: only sys, pathlib.Path, pytest imports plus sys.path.insert(0, str(Path(__file__).parent))
    - Inside each test function body, before calling main(): `from validate_spec import main`
    - This allows `pytest --co` to collect all 6 tests while execution fails with ImportError until validate_spec.py is written (RED state)

    Commit message: test(02-01): add failing tests for tool-validate-spec exit-code contract
  </action>

  <acceptance_criteria>
    - mise-en-place/tool-validate-spec/test_validate_spec.py exists
    - File contains exactly six functions named: test_approved_spec_exits_zero, test_draft_spec_exits_one, test_missing_spec_exits_one, test_malformed_frontmatter_exits_one, test_approved_spec_has_required_sections, test_approved_spec_missing_section_exits_one
    - File contains "## Who It's For" and "## Out of Scope" (canonical header strings — not "Who it's for" or "Out-of-scope")
    - File contains sys.path.insert (imports validate_spec from same directory)
    - File contains monkeypatch.chdir (controls working directory per test)
    - Each test function contains `from validate_spec import main` (lazy import inside function body)
    - Running python3 -m pytest mise-en-place/tool-validate-spec/test_validate_spec.py -x -q exits non-zero (tests fail — validate_spec.py missing or incomplete)
  </acceptance_criteria>

  <verify>
    <automated>grep -cE '^def test_' mise-en-place/tool-validate-spec/test_validate_spec.py && python3 -m pytest mise-en-place/tool-validate-spec/test_validate_spec.py --co -q 2>&1 | grep -c "test_"</automated>
  </verify>

  <done>test_validate_spec.py exists with 6 test functions; grep confirms 6 def test_ lines; pytest --co collects all 6; running tests exits non-zero (RED confirmed); committed with message test(02-01): add failing tests for tool-validate-spec exit-code contract</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Write validate_spec.py — make all six tests pass</name>
  <files>mise-en-place/tool-validate-spec/validate_spec.py</files>

  <read_first>
    - mise-en-place/tool-validate-spec/test_validate_spec.py (the exact expectations each test asserts — implement against these)
    - mise-en-place/tool-env-check/env_check.py (stdlib-only exit-code pattern: pathlib, sys.exit, print to stderr)
    - mise-en-place/tool-planning-scaffold/planning_scaffold.py (pathlib patterns)
    - .planning/phases/02-spec-phase-greenfield/02-RESEARCH.md (Pattern 2: YAML front-matter parser design; Pitfall 2 CRLF warning)
    - .planning/phases/02-spec-phase-greenfield/02-REVIEWS.md (HIGH: REQUIRED_SECTIONS body scan required in main())
  </read_first>

  <action>
    Write mise-en-place/tool-validate-spec/validate_spec.py with:

    Module docstring: "tool-validate-spec — Check that SPEC.md exists, has status: approved, and contains all required section headers. Exit 0: approved with valid sections. Exit 1: not approved, missing file, malformed front-matter, or missing section (message to stderr). Required sections (exact match): ## Problem, ## Who It's For, ## Constraints, ## Success Criteria, ## Out of Scope"

    Imports: pathlib, sys (stdlib only — no yaml, no tomllib, no third-party imports).

    SPEC_CANDIDATES constant: [pathlib.Path(".planning/SPEC.md"), pathlib.Path("SPEC.md")] — searched in order.

    REQUIRED_SECTIONS constant (exact strings, case-sensitive):
    ["## Problem", "## Who It's For", "## Constraints", "## Success Criteria", "## Out of Scope"]
    These must match spec-phase/SKILL.md Step 4 template headers exactly (plan 02-02 cross-reference).

    parse_frontmatter(text: str) -> dict function:
    - Returns {} if text does not start with "---"
    - Finds closing front-matter boundary with text.find("\n---", 3); returns {} if -1
    - Extracts block between the two "---" markers using text[3:end].strip()
    - Iterates block.splitlines() (not split("\n") — CRLF safety)
    - Skips blank lines and lines starting with "#"
    - For lines containing ":", calls line.partition(":") to split key and value; strips whitespace from both
    - Returns dict of parsed key→value pairs

    main() function:
    - Iterates SPEC_CANDIDATES; breaks on first candidate where .exists() is True; spec_path remains None if none found
    - If spec_path is None: prints to sys.stderr "ERROR: SPEC.md not found in .planning/ or project root.\nRun /spec-phase first to create and approve a spec." then calls sys.exit(1)
    - Reads text = spec_path.read_text(encoding="utf-8"); calls parse_frontmatter(text)
    - If result dict is empty: prints to sys.stderr "ERROR: {spec_path} has no valid YAML front-matter.\nExpected format:\n---\nstatus: approved\nversion: 1\ndate: YYYY-MM-DD\n---" then sys.exit(1)
    - Gets fm.get("status", ""); if != "approved": prints to sys.stderr "ERROR: SPEC.md status is '{status}' — must be 'approved' before planning.\nRun /spec-phase and complete the approval gate." then sys.exit(1)
    - After status passes: for each header in REQUIRED_SECTIONS, if header not in text: print to sys.stderr "ERROR: SPEC.md missing required section: {header}" then sys.exit(1). Use substring `in text` check (header must appear verbatim in file body).
    - On success: prints "✓ SPEC.md approved (version {fm.get('version','?')}, {fm.get('date','?')})" then sys.exit(0)

    if __name__ == "__main__": main()

    Commit message: feat(02-01): implement validate_spec.py — SPEC.md approval gate
  </action>

  <acceptance_criteria>
    - mise-en-place/tool-validate-spec/validate_spec.py exists
    - File contains def parse_frontmatter(
    - File contains REQUIRED_SECTIONS
    - File contains "## Who It's For" inside REQUIRED_SECTIONS (canonical header documented in source)
    - File contains sys.exit(0) and sys.exit(1) (both exit codes present)
    - File contains .splitlines() (not .split("\n") — CRLF safety enforced)
    - File contains "missing required section" (section validation error path)
    - File contains no import outside stdlib (no yaml, no tomllib, no requests)
    - python3 -m pytest mise-en-place/tool-validate-spec/ -x -q exits 0 (all 6 tests GREEN)
    - python3 mise-en-place/tool-validate-spec/validate_spec.py run from a directory with no SPEC.md exits 1 and prints "not found" to stderr
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest mise-en-place/tool-validate-spec/ -x -q</automated>
  </verify>

  <done>All 6 tests pass (GREEN confirmed); validate_spec.py committed with message feat(02-01): implement validate_spec.py — SPEC.md approval gate</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → validate_spec.py | Script reads SPEC.md from the working directory; attacker-controlled content enters via file read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | SPEC.md manual edit to bypass gate | accept | validate_spec.py reads the file as written; developers own their local files. This tool enforces workflow discipline, not security access control. |
| T-02-02 | Information Disclosure | stderr error messages | accept | Error messages emit status values, section names, and path strings only — no file contents, no secrets, no PII. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — stdlib-only. Supply chain risk is zero. |
</threat_model>

<verification>
After both tasks complete and tests are green:

```shell
python3 -m pytest mise-en-place/tool-validate-spec/ -x -q
python3 -m pytest mise-en-place/ -q
```

Both commands must exit 0.
</verification>

<success_criteria>
- python3 -m pytest mise-en-place/tool-validate-spec/ -x -q exits 0 (6/6 tests pass)
- python3 mise-en-place/tool-validate-spec/validate_spec.py run with no SPEC.md in scope exits 1 with "not found" in stderr
- validate_spec.py contains REQUIRED_SECTIONS with exact canonical headers matching spec-phase template
- validate_spec.py contains no non-stdlib imports (grep -n "^import\|^from" validate_spec.py shows only pathlib, sys)
- Two atomic commits: one for tests (RED), one for implementation (GREEN)
</success_criteria>

<output>
Create .planning/phases/02-spec-phase-greenfield/02-01-SUMMARY.md when done
</output>
