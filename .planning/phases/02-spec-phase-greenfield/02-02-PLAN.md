---
phase: 02-spec-phase-greenfield
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - mise-en-place/tool-validate-spec/SKILL.md
  - mise-en-place/spec-phase/SKILL.md
  - mise-en-place/README.md
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04
  - SPEC-05
  - PROJ-01

must_haves:
  truths:
    - "Developer can run /spec-phase on an empty repo and receive a Q&A covering exactly five categories in order: Problem, Who it's for, Constraints, Success Criteria, Out-of-scope"
    - "Completing the Q&A produces .planning/SPEC.md with YAML front-matter (status: draft, version, date) and all five mandatory sections using canonical headers: ## Problem, ## Who It's For, ## Constraints, ## Success Criteria, ## Out of Scope"
    - "Developer can trigger research sub-tasks inline (say 'research X') or at end of Q&A; each writes to .planning/research/RESEARCH-<topic-slug>.md"
    - "The approval gate asks 'Approve this spec? (yes / edit / abort)'; 'yes' patches status: draft → status: approved in-place via StrReplace"
    - "Running python3 mise-en-place/tool-validate-spec/validate_spec.py exits 0 only after the approval gate succeeds and all section headers are present"
    - "spec-phase can be invoked with zero prior session history — startup reads only local files"
    - "spec-phase works on a repo where .planning/ does not yet exist — startup scaffolds it and creates .planning/research/"
    - "mise-en-place/README.md documents tool-validate-spec and spec-phase with invocation one-liners"
  artifacts:
    - path: "mise-en-place/tool-validate-spec/SKILL.md"
      provides: "Agent-facing invocation contract for validate_spec.py including section validation"
      contains: "python3 mise-en-place/tool-validate-spec/validate_spec.py"
    - path: "mise-en-place/spec-phase/SKILL.md"
      provides: "Full agent workflow: startup, Q&A, research, SPEC.md assembly, approval gate"
      contains: "## Step 1: Startup"
    - path: "mise-en-place/README.md"
      provides: "Tool catalog listing all mise-en-place skills including Phase 2 additions"
      contains: "tool-validate-spec"
  key_links:
    - from: "spec-phase/SKILL.md:Step 1"
      to: "mise-en-place/tool-planning-scaffold/planning_scaffold.py"
      via: "Shell call: python3 mise-en-place/tool-planning-scaffold/planning_scaffold.py scaffold"
      pattern: "planning_scaffold.py scaffold"
    - from: "spec-phase/SKILL.md:Step 1"
      to: ".planning/research/"
      via: "Shell call: mkdir -p .planning/research (scaffold does not create research/ — verified in planning_scaffold.py PLANNING_DIRS)"
      pattern: "mkdir -p .planning/research"
    - from: "spec-phase/SKILL.md:Step 4"
      to: "validate_spec.py:REQUIRED_SECTIONS"
      via: "exact section header strings must match validator"
      pattern: "## Who It's For"
    - from: "spec-phase/SKILL.md:Step 5 (approval gate)"
      to: ".planning/SPEC.md"
      via: "StrReplace old_string 'status: draft' new_string 'status: approved'"
      pattern: "StrReplace"
    - from: "spec-phase/SKILL.md:research steps"
      to: ".planning/research/RESEARCH-<topic-slug>.md"
      via: "Task tool spawning generalPurpose sub-agent without model parameter"
      pattern: "Task tool"
    - from: "tool-validate-spec/SKILL.md"
      to: "mise-en-place/tool-validate-spec/validate_spec.py"
      via: "Shell: python3 mise-en-place/tool-validate-spec/validate_spec.py"
      pattern: "validate_spec.py"
---

<objective>
Deliver both SKILL.md agent contracts for Phase 2 plus README documentation: the tool-validate-spec invocation contract (slim, one-page), the spec-phase workflow skill (comprehensive, all five steps), and mise-en-place/README.md entries for the new tools.

Purpose: These are the user-facing deliverables of the phase. After this plan executes, a developer can invoke /spec-phase on any repo — empty or otherwise — and be guided from nothing to an approved SPEC.md. README satisfies TOOL-05 self-contained skill folder discoverability.

Output: mise-en-place/tool-validate-spec/SKILL.md + mise-en-place/spec-phase/SKILL.md + updated mise-en-place/README.md.

Addresses review concerns: README gap (MEDIUM), research/ dir creation (MEDIUM), D-04 path override note (LOW), canonical section headers aligned with validator (MEDIUM), no model param on research sub-agent (LOW).
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-spec-phase-greenfield/02-CONTEXT.md
@.planning/phases/02-spec-phase-greenfield/02-RESEARCH.md
@.planning/phases/02-spec-phase-greenfield/02-REVIEWS.md
@.planning/phases/02-spec-phase-greenfield/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from Phase 1 deliverables and plan 02-01 output. -->

From mise-en-place/tool-planning-scaffold/planning_scaffold.py:
  PLANNING_DIRS = [".planning", ".planning/phases", ".planning/codebase"] — does NOT include .planning/research/
  Subcommands: scaffold | write-artifact | read-artifact | format-file
  Scaffold invocation: python3 mise-en-place/tool-planning-scaffold/planning_scaffold.py scaffold
  Creates: .planning/, .planning/phases/, .planning/codebase/ (idempotent)

From mise-en-place/tool-validate-spec/validate_spec.py (from plan 02-01):
  Invocation: python3 mise-en-place/tool-validate-spec/validate_spec.py
  Exit 0: SPEC.md found, status == approved, all REQUIRED_SECTIONS present; prints "✓ SPEC.md approved (version N, YYYY-MM-DD)"
  Exit 1: SPEC.md missing, malformed front-matter, status != approved, or missing section header; prints actionable message to stderr
  REQUIRED_SECTIONS (exact strings — spec-phase Step 4 must use these verbatim):
    ## Problem
    ## Who It's For
    ## Constraints
    ## Success Criteria
    ## Out of Scope
  No arguments required; searches .planning/SPEC.md then SPEC.md relative to CWD
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write tool-validate-spec/SKILL.md — agent-facing invocation contract</name>
  <files>mise-en-place/tool-validate-spec/SKILL.md</files>

  <read_first>
    - mise-en-place/tool-env-check/SKILL.md (exact SKILL.md structure to replicate: frontmatter keys, cursor_skill_adapter block, objective block, process block)
    - mise-en-place/tool-validate-spec/validate_spec.py (exact CLI command, exit codes, REQUIRED_SECTIONS constant, stdout/stderr behavior to document)
  </read_first>

  <action>
    Create mise-en-place/tool-validate-spec/SKILL.md following the exact same structural pattern as mise-en-place/tool-env-check/SKILL.md.

    Frontmatter: name: tool-validate-spec, description: "Check that SPEC.md exists, has status: approved, and contains all required sections before planning begins"

    cursor_skill_adapter block: identical structure to tool-env-check — Sections A through D covering invocation trigger (tool-validate-spec mention), GSD_ARGS handling, Cursor tool usage (Shell, Read, Write, etc.), and subagent spawning via Task tool.

    objective block: "Verify that .planning/SPEC.md exists, has status: approved, and contains all five required section headers. Exits 0 on success, exits 1 with an actionable error message on any failure."

    process block:
    - Show the exact invocation command: python3 mise-en-place/tool-validate-spec/validate_spec.py
    - Document exit code contract: Exit 0 = SPEC.md found, approved, and all required sections present. Exit 1 = SPEC.md missing, malformed front-matter, status not yet approved, or missing required section header.
    - List the five required section headers exactly (must match validate_spec.py REQUIRED_SECTIONS): ## Problem, ## Who It's For, ## Constraints, ## Success Criteria, ## Out of Scope
    - Describe when this tool is invoked: "Run this before /plan-phase begins. If exit code is non-zero, the developer must complete /spec-phase and reach the approval gate before planning can proceed."
    - Show example success output: ✓ SPEC.md approved (version 1, 2026-05-21)
    - Show example failure cases and their stderr messages (not found, draft status, malformed front-matter, missing required section)

    Commit message: feat(02-02): add tool-validate-spec/SKILL.md — invocation contract
  </action>

  <acceptance_criteria>
    - mise-en-place/tool-validate-spec/SKILL.md exists
    - File contains name: tool-validate-spec in frontmatter
    - File contains cursor_skill_adapter (the adapter block is present)
    - File contains python3 mise-en-place/tool-validate-spec/validate_spec.py (exact invocation command)
    - File contains "Exit 0" and "Exit 1" (exit code contract documented)
    - File contains "## Who It's For" (canonical section header documented — matches validator)
    - File contains "missing required section" or equivalent section-validation failure description
    - File contains /spec-phase (cross-reference to the upstream workflow that produces the approved spec)
    - File does not reference any pip install or non-stdlib dependency
  </acceptance_criteria>

  <verify>
    <automated>grep -q "cursor_skill_adapter" mise-en-place/tool-validate-spec/SKILL.md && grep -q "python3 mise-en-place/tool-validate-spec/validate_spec.py" mise-en-place/tool-validate-spec/SKILL.md && grep -q "Exit 0" mise-en-place/tool-validate-spec/SKILL.md</automated>
  </verify>

  <done>tool-validate-spec/SKILL.md committed; grep confirms name: tool-validate-spec, validate_spec.py invocation, and ## Who It's For canonical header present</done>
</task>

<task type="auto">
  <name>Task 2: Write spec-phase/SKILL.md — full agent workflow (startup → Q&A → research → assembly → approval)</name>
  <files>mise-en-place/spec-phase/SKILL.md</files>

  <read_first>
    - mise-en-place/tool-env-check/SKILL.md (cursor_skill_adapter block structure — replicate exactly)
    - mise-en-place/tool-planning-scaffold/SKILL.md (how planning_scaffold.py scaffold subcommand is documented)
    - mise-en-place/tool-planning-scaffold/planning_scaffold.py (PLANNING_DIRS — confirm research/ is NOT created by scaffold)
    - mise-en-place/tool-validate-spec/validate_spec.py (REQUIRED_SECTIONS — Step 4 headers must match exactly)
    - .planning/phases/02-spec-phase-greenfield/02-CONTEXT.md (D-01 through D-09 locked decisions — every decision must appear in a step)
    - .planning/phases/02-spec-phase-greenfield/02-RESEARCH.md (Pattern 3 research sub-task prompt template; Pattern 4 approval gate; Pitfall 1 portable paths; Pitfall 4 existing SPEC.md; Pitfall 5 research naming; Pitfall 6 idempotent approval)
    - .planning/phases/02-spec-phase-greenfield/02-REVIEWS.md (research/ mkdir, D-04 override, no model param, canonical headers)
  </read_first>

  <action>
    Create mise-en-place/spec-phase/ directory (via first Write call). Write mise-en-place/spec-phase/SKILL.md with the following structure:

    FRONTMATTER: name: spec-phase, description: "Structured deep-questioning flow → .planning/SPEC.md → explicit approval gate"

    CURSOR_SKILL_ADAPTER: identical structure to tool-env-check/SKILL.md — Sections A through D. Invocation trigger: user mentions spec-phase or describes starting a new project spec.

    OBJECTIVE block: "Take a developer from any starting point — including an empty repo — to an explicitly approved SPEC.md, using a structured Q&A that covers five mandatory categories, supports optional research sub-tasks, and enforces an in-flow approval gate before planning can begin."

    PROCESS block with exactly five numbered steps:

    STEP 1 — STARTUP (per D-09, PROJ-01, SPEC-05):
    Run: python3 mise-en-place/tool-planning-scaffold/planning_scaffold.py scaffold
    This creates .planning/, .planning/phases/, .planning/codebase/ idempotently. Note: planning_scaffold.py does NOT create .planning/research/ (PLANNING_DIRS omits it — verified in source).
    Then run: mkdir -p .planning/research
    This ensures the research output directory exists before any research sub-task writes files. Addresses review concern: MEDIUM research/ dir creation gap.
    Then Read .planning/SPEC.md. If it exists: present its status value and ask: "An existing spec was found (status: <status>). What would you like to do? (1) Continue editing from the last completed section  (2) Start fresh — discard and restart  (3) Abort". On "3": stop immediately. On "1": resume Q&A from first incomplete category. On "2": overwrite with a new session.
    Clean-context note: all context comes from local files read here. No session history is assumed.

    STEP 2 — Q&A FLOW — FIVE-CATEGORY SPINE (per D-01, D-02, SPEC-01):
    Always cover all five categories in this exact order: (1) Problem, (2) Who it's for, (3) Constraints, (4) Success Criteria, (5) Out-of-scope.
    For each category: open with a focused starting question. After each answer, evaluate: is it specific enough to write a concrete spec section? (Specific = names facts, conditions, or numbers. Vague = abstract generalities or "it depends.") If vague and fewer than 3 turns used in this category: ask one targeted follow-up probe. Advance to the next category when: (a) 3 turns are spent in this category, OR (b) the answer is specific enough — the agent decides, not the user (per D-02).
    Inline research trigger (per D-03b): at any point during Q&A, if the user says "research X" or asks to look something up, immediately execute Step 3 for that topic, then resume Q&A from where it left off.

    STEP 3 — RESEARCH SUB-TASKS (per D-03, D-04, D-05, SPEC-03):
    Two trigger modes — handle both identically once triggered:
    (a) User-requested: user says "research <topic>" at any point during Q&A — trigger immediately.
    (b) Agent-suggested: at the end of Q&A, if knowledge gaps were detected, present a numbered list of proposed research topics and ask the user to accept or skip each individually.
    Before spawning any sub-agent: ensure .planning/research/ exists (run mkdir -p .planning/research if Step 1 was skipped or directory was removed).
    For each accepted research topic: use the Task tool to spawn a generalPurpose sub-agent. Do NOT pass the model parameter — use the Cursor default model. Addresses review concern: LOW model guidance.
    The sub-agent prompt must include:
    — Topic: <the research topic>
    — Project context: <1-2 sentence summary of what the user is building, drawn from Q&A answers so far>
    — Output file: .planning/research/RESEARCH-<topic-slug>.md where topic-slug is the topic lowercased and hyphen-separated (e.g. "stripe-pricing-model").
    — Path override note (D-04): CONTEXT.md D-04 originally specified .planning/phases/02-spec-phase-greenfield/ for research output. That path is phase-specific and non-portable when this skill is replicated to new projects. This skill uses .planning/research/ instead per RESEARCH.md Pitfall 1. Addresses review concern: LOW D-04 path override documentation.
    — File format: # Research: <topic>, then sections: ## Summary (2-3 sentence executive summary), ## Key Findings (bullets), ## Implications for Spec (how findings shape constraints or success criteria), ## Sources (URLs or descriptions)
    — Instruction: "Keep the file concise — the spec author will read this before writing the final spec."
    After each sub-agent completes: Read .planning/research/RESEARCH-<topic-slug>.md and note the key findings for incorporation into SPEC.md.
    Multiple research sub-tasks may run; each produces its own file. All are read before the spec is finalized.

    STEP 4 — SPEC.MD ASSEMBLY (per D-06, D-07, SPEC-02):
    Merge all Q&A answers and all research findings. Use the Write tool to create .planning/SPEC.md with this exact template structure:
    Front-matter block: three-line block delimited by --- markers, containing: status: draft, version: 1, date: <today's date in YYYY-MM-DD format>
    Document body: # Spec: <project name from Q&A>, then five sections in this exact order with these exact header strings (must match validate_spec.py REQUIRED_SECTIONS — case-sensitive):
      ## Problem
      ## Who It's For
      ## Constraints
      ## Success Criteria
      ## Out of Scope
    Each section contains the concrete answers gathered in the Q&A, enriched with research findings where relevant. Q&A category names use conversational casing ("Who it's for", "Out-of-scope") but the written SPEC.md headers must use the canonical forms above.

    STEP 5 — APPROVAL GATE (per D-08, D-09, SPEC-04):
    Present the full contents of .planning/SPEC.md to the developer.
    Ask: "Approve this spec? Reply with: yes / edit / abort"
    On "yes": check if "status: draft" is present in the front-matter. If yes: use StrReplace on .planning/SPEC.md with old_string "status: draft" and new_string "status: approved". If already "status: approved" (idempotent case): skip the patch. Confirm: "Spec approved ✓ — run /plan-phase to begin planning." Remind: "The plan-phase will verify approval via python3 mise-en-place/tool-validate-spec/validate_spec.py before proceeding."
    On "edit": ask "Which section would you like to revise? (Problem / Who it's for / Constraints / Success Criteria / Out-of-scope)". Return to Q&A for that category. Re-assemble and re-present SPEC.md. Re-prompt approval.
    On "abort": leave status as draft. Confirm: "Spec saved as draft in .planning/SPEC.md — resume with /spec-phase." Exit.

    Commit message: feat(02-02): add spec-phase/SKILL.md — full spec workflow
  </action>

  <acceptance_criteria>
    - mise-en-place/spec-phase/SKILL.md exists
    - File contains name: spec-phase in frontmatter
    - File contains cursor_skill_adapter (adapter block present — same pattern as tool-env-check)
    - File contains ## Step 1 (or Step 1:) and ## Step 2 (or Step 2:) through Step 5 (five numbered steps present)
    - File contains planning_scaffold.py scaffold (startup scaffolding command)
    - File contains mkdir -p .planning/research (explicit research directory creation — scaffold alone is insufficient)
    - File contains "Problem" AND "Who it's for" AND "Constraints" AND "Success Criteria" AND "Out-of-scope" (all five Q&A category names present)
    - File contains "## Who It's For" (canonical SPEC.md section header matching validator)
    - File contains "## Out of Scope" (canonical SPEC.md section header matching validator)
    - File contains "3 turns" or "three turns" (D-02 advancement rule documented)
    - File contains RESEARCH- (research file pattern for portable file naming)
    - File contains .planning/research/ (correct research output directory — not a hardcoded phase path)
    - File contains status: draft (SPEC.md template front-matter)
    - File contains status: approved (approval gate target value)
    - File contains StrReplace (approval gate patching mechanism)
    - File contains tool-validate-spec (D-09 belt-and-suspenders reference)
    - File contains "Do NOT pass the model parameter" or equivalent (default model for research sub-agents)
    - File contains D-04 path override note or ".planning/research/ instead" portability rationale
    - File does NOT contain phases/02-spec-phase-greenfield in any research output file path (portability: no hardcoded phase directory)
    - python3 -m pytest mise-en-place/tool-validate-spec/ -x -q still exits 0 (no regressions from plan 02-01)
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest mise-en-place/tool-validate-spec/ -x -q && grep -c "mkdir -p .planning/research" mise-en-place/spec-phase/SKILL.md</automated>
  </verify>

  <done>spec-phase/SKILL.md committed; all acceptance criteria grep-verified; tests still green; commit message feat(02-02): add spec-phase/SKILL.md — full spec workflow</done>
</task>

<task type="auto">
  <name>Task 3: Update mise-en-place/README.md — document Phase 2 tools</name>
  <files>mise-en-place/README.md</files>

  <read_first>
    - mise-en-place/README.md (existing tool entries for tool-env-check, tool-context-builder, tool-planning-scaffold — match format and tone)
    - mise-en-place/tool-validate-spec/SKILL.md (Task 1 output — invocation one-liner)
    - mise-en-place/spec-phase/SKILL.md (Task 2 output — workflow description)
  </read_first>

  <action>
    Update mise-en-place/README.md by adding two new tool sections under ## Tools, following the same format as existing entries (### heading, one-paragraph description, no duplicate Usage section content).

    Add ### tool-validate-spec section:
    - Description: Validates that .planning/SPEC.md exists, has status: approved, and contains all five required section headers before planning begins. Agent reads tool-validate-spec/SKILL.md for invocation contract.
    - One-liner example: python3 mise-en-place/tool-validate-spec/validate_spec.py

    Add ### spec-phase section:
    - Description: Structured deep-questioning workflow that guides a developer from an empty repo to an explicitly approved SPEC.md. Covers five Q&A categories, optional research sub-tasks, and an in-flow approval gate. Agent reads spec-phase/SKILL.md — no Python script (workflow skill only).
    - Invocation note: invoked via /spec-phase or by mentioning spec-phase in conversation (no CLI script).

    Do not remove or modify existing tool entries. Addresses review concern: MEDIUM README gap for TOOL-05 self-contained skill folder.

    Commit message: docs(02-02): add tool-validate-spec and spec-phase to mise-en-place README
  </action>

  <acceptance_criteria>
    - mise-en-place/README.md contains ### tool-validate-spec heading
    - mise-en-place/README.md contains ### spec-phase heading
    - File contains python3 mise-en-place/tool-validate-spec/validate_spec.py (validate-spec invocation one-liner)
    - File contains spec-phase/SKILL.md (spec-phase skill reference)
    - Existing ### tool-env-check, ### tool-context-builder, ### tool-planning-scaffold sections remain present
  </acceptance_criteria>

  <verify>
    <automated>grep -q "### tool-validate-spec" mise-en-place/README.md && grep -q "### spec-phase" mise-en-place/README.md && grep -q "validate_spec.py" mise-en-place/README.md</automated>
  </verify>

  <done>README.md updated with both Phase 2 tool entries; committed with message docs(02-02): add tool-validate-spec and spec-phase to mise-en-place README</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → agent | Agent reads SKILL.md as authoritative instructions; content shapes all downstream actions including sub-agent spawning |
| agent → Task tool | Research sub-agent receives a prompt constructed from user Q&A answers; prompt content is developer-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | spec-phase SKILL.md instructions modified to skip approval gate | accept | Developer controls local files; this tool enforces workflow discipline, not security access control. validate_spec.py provides the hard exit-code gate regardless of SKILL.md state. |
| T-02-04 | Spoofing | Research sub-agent prompt crafted to exfiltrate project context via external web calls | accept | Cursor Task tool is sandboxed; sub-agents are generalPurpose agents writing to local files. The research prompt template explicitly instructs "write findings to .planning/research/..." — no external service credentials are passed. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — SKILL.md files are markdown only. Supply chain risk is zero. |
</threat_model>

<verification>
After all three tasks commit:

```shell
# Validate test suite still green (no regressions)
python3 -m pytest mise-en-place/ -q

# Confirm required strings in spec-phase SKILL.md
grep -c "Problem\|Who it's for\|Constraints\|Success Criteria\|Out-of-scope" mise-en-place/spec-phase/SKILL.md

# Confirm portable research path (no hardcoded phase dir in output paths)
grep -c "phases/02-spec-phase-greenfield" mise-en-place/spec-phase/SKILL.md
# Must output 0 (D-04 override note may mention path in explanatory text — acceptable if not used as write target)

# Confirm README documents new tools
grep -c "tool-validate-spec\|spec-phase" mise-en-place/README.md
```
</verification>

<success_criteria>
- python3 -m pytest mise-en-place/ -q exits 0
- mise-en-place/tool-validate-spec/SKILL.md exists with validate_spec.py invocation and section header requirements documented
- mise-en-place/spec-phase/SKILL.md exists with all five Q&A categories, both research trigger modes, mkdir -p .planning/research, canonical SPEC.md headers matching REQUIRED_SECTIONS, and approval gate
- mise-en-place/README.md lists tool-validate-spec and spec-phase alongside Phase 1 tools
- No hardcoded phase-specific paths used as research write targets in spec-phase/SKILL.md
- Three atomic commits (one per task)
</success_criteria>

<output>
Create .planning/phases/02-spec-phase-greenfield/02-02-SUMMARY.md when done
</output>
