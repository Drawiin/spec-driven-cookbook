---
phase: 04-plan-phase
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - mise-en-place/tool-validate-plan/validate_plan.py
  - mise-en-place/tool-validate-plan/test_validate_plan.py
  - mise-en-place/tool-validate-plan/SKILL.md
autonomous: true
requirements:
  - PLAN-02
  - PLAN-04

must_haves:
  truths:
    - "python3 mise-en-place/tool-validate-plan/validate_plan.py --phase-dir .planning/phases/{slug} exits 0 when all *-PLAN.md files have status: approved, ≥1 todo task line, and each task has a Verify: sub-bullet"
    - "validate_plan.py exits 1 with actionable stderr when any plan has status: draft"
    - "validate_plan.py exits 1 when a task line lacks a Verify: sub-bullet in its indented block"
    - "validate_plan.py exits 1 when a plan has no [STATUS] TASKID todo lines"
    - "validate_plan.py exits 1 when --phase-dir is missing, outside cwd, or contains no *-PLAN.md files"
    - "python3 -m pytest mise-en-place/tool-validate-plan/ -x -q exits 0 (all test functions pass)"
    - "tool-validate-plan/SKILL.md documents invocation, exit codes, and todo-format validation scope per D-11"
  artifacts:
    - path: "mise-en-place/tool-validate-plan/validate_plan.py"
      provides: "Stdlib-only PLAN.md approval + todo-format validator with --phase-dir"
      contains: "def parse_frontmatter"
    - path: "mise-en-place/tool-validate-plan/test_validate_plan.py"
      provides: "pytest coverage for exit-code behaviors and todo/Verify presence checks"
      contains: "def test_all_approved_valid_plans_exits_zero"
    - path: "mise-en-place/tool-validate-plan/SKILL.md"
      provides: "Agent-facing invocation contract for validate_plan.py"
      contains: "python3 mise-en-place/tool-validate-plan/validate_plan.py"
  key_links:
    - from: "validate_plan.py:main()"
      to: ".planning/phases/{slug}/*-PLAN.md"
      via: "glob **/*-PLAN.md under --phase-dir"
      pattern: "glob.*PLAN.md"
    - from: "validate_plan.py:parse_frontmatter()"
      to: "PLAN.md front-matter block"
      via: "copied from validate_spec.py — flat key:value parser"
      pattern: "def parse_frontmatter"
    - from: "validate_plan.py:iter_tasks()"
      to: "todo task lines"
      via: "TASK_LINE regex matching [PENDING|IN_PROGRESS|DONE] TASKID - Title"
      pattern: "TASK_LINE"
    - from: "test_validate_plan.py"
      to: "validate_plan.py"
      via: "sys.path.insert(0, ...) then from validate_plan import main"
      pattern: "sys.path.insert"
---

## Phase Goal

**As a** developer, **I want to** transform any approved spec into parallel-ready atomic task plans, **so that** I can execute work with explicit approval gates and verifiable deliverables per task.

<objective>
Deliver the Python validation gate for todo-format PLAN.md approval status and task/Verify presence: implementation file, full test suite (TDD RED→GREEN), and agent invocation contract.

Purpose: D-19 belt-and-suspenders enforcement — exec-phase (Phase 5) and plan-phase approval confirmation cannot proceed unless `validate_plan.py` exits 0. D-11 locks validator scope to presence-only checks (todo pattern + Verify sub-bullet + status approved) — not atomicity sizing or Mermaid syntax.

Output: `mise-en-place/tool-validate-plan/validate_plan.py` + `test_validate_plan.py` + `tool-validate-plan/SKILL.md`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-plan-phase/04-CONTEXT.md
@.planning/phases/04-plan-phase/04-RESEARCH.md
@.planning/phases/04-plan-phase/04-PATTERNS.md

<interfaces>
From mise-en-place/tool-validate-spec/validate_spec.py (copy patterns verbatim):
- parse_frontmatter(text: str) -> dict — flat YAML between --- markers; skip blank/comment lines; line.partition(':')
- Exit contract: sys.exit(0) on success (stdout confirmation), sys.exit(1) on failure (ERROR: prefix to stderr)
- Status gate: fm.get("status") must equal "approved"

From mise-en-place/tool-planning-scaffold/planning_scaffold.py:
- _validate_root(raw_root) -> Path — resolve path; reject if outside cwd (mirror for --phase-dir)

Locked todo format (D-06 through D-11):
- Task line: `[PENDING|IN_PROGRESS|DONE] {plan}-{task} - Title` e.g. `[PENDING] 04-01-T1 - Create validate_plan.py`
- Verify sub-bullet: `  - [ ] Verify: <observable outcome>` (indented under task)
- Minimal frontmatter (D-15): status, phase, plan, requirements[]
</interfaces>
</context>

<feature>
  <name>tool-validate-plan — PLAN.md approval + todo-format gate (stdlib-only Python)</name>
  <files>mise-en-place/tool-validate-plan/validate_plan.py, mise-en-place/tool-validate-plan/test_validate_plan.py, mise-en-place/tool-validate-plan/SKILL.md</files>
  <behavior>
    Behaviors verified by test functions:
    - test_all_approved_valid_plans_exits_zero: single approved plan with valid todo + Verify → exit 0
    - test_all_approved_multi_plan_exits_zero: two approved plans in phase dir → exit 0
    - test_draft_plan_exits_one: plan with status: draft → exit 1, stderr contains "must be 'approved'"
    - test_one_draft_plan_fails_all: two plans, one draft → exit 1 (PLAN-03 multi-plan gate)
    - test_missing_verify_subbullet_exits_one: task line without Verify in indented block → exit 1
    - test_no_tasks_exits_one: plan body with no [STATUS] TASKID lines → exit 1
    - test_malformed_frontmatter_exits_one: plan without --- front-matter → exit 1
    - test_missing_phase_dir_exits_one: --phase-dir path does not exist → exit 1
    - test_no_plan_files_exits_one: phase dir exists but no *-PLAN.md → exit 1
    - test_phase_dir_outside_cwd_exits_one: --phase-dir resolves outside cwd → exit 1

    All tests must be RED before validate_plan.py exists.
  </behavior>
  <implementation>
    validate_plan.py uses pathlib, re, sys, argparse only (stdlib).
    CLI: required --phase-dir PATH argument.
    Path validation: resolve --phase-dir; reject if outside cwd (mirror planning_scaffold._validate_root pattern).
    Glob all *-PLAN.md files recursively under phase dir; fail if none found.
    For each plan file: parse_frontmatter (copy from validate_spec.py); check status == "approved"; scan body for task lines via TASK_LINE regex; for each task, scan subsequent indented lines until next task line or ## header — require at least one line containing "Verify:".
    Success stdout: e.g. "✓ 2 plan(s) approved in .planning/phases/04-plan-phase/"
    Do NOT validate: Mermaid syntax, task atomicity/sizing, wave assignment, cross-plan depends_on (D-05/D-11 scope).
  </implementation>
</feature>

<tasks>

<task type="tdd" phase="red">
  <name>RED: Write test_validate_plan.py — failing test suite for exit-code contract</name>
  <files>mise-en-place/tool-validate-plan/test_validate_plan.py</files>

  <read_first>
    - mise-en-place/tool-validate-spec/test_validate_spec.py (fixture helpers _spec_body, tmp_path + monkeypatch.chdir, SystemExit assertions, capsys stderr)
    - .planning/phases/04-plan-phase/04-RESEARCH.md (Code Examples: TASK_LINE regex, Verify block scan, CLI --phase-dir)
    - .planning/phases/04-plan-phase/04-PATTERNS.md (test_validate_plan.py analog table — six+ test cases)
    - .planning/phases/04-plan-phase/04-CONTEXT.md (D-06 through D-11 locked task format)
  </read_first>

  <action>
    Create mise-en-place/tool-validate-plan/ directory (via first Write call). Write test_validate_plan.py with:

    Imports: sys, pathlib.Path, pytest. sys.path.insert(0, str(Path(__file__).parent)) at module level.

    Helper _plan_body(status="approved", plan_num="01", tasks=None) returning minimal valid todo-format PLAN.md string per D-06/D-15:
    - Frontmatter: status, phase: 04-plan-phase, plan: {plan_num}, requirements list with PLAN-01
    - Body: ## Objective, ## Tasks section
    - Default single task: `[PENDING] 04-01-T1 - Example task` with indented `  - [ ] Verify: observable outcome`

    Helper _write_plan(phase_dir, filename, content) to write plan file under tmp_path phase directory.

    Ten test functions — each uses monkeypatch.chdir(tmp_path), creates phase dir structure, calls main(["--phase-dir", str(phase_path)]) inside pytest.raises(SystemExit):

    1. test_all_approved_valid_plans_exits_zero — one approved plan with valid task + Verify → code 0
    2. test_all_approved_multi_plan_exits_zero — 04-01-PLAN.md and 04-02-PLAN.md both approved with tasks → code 0 (per D-16 multi-plan approve-all validation path)
    3. test_draft_plan_exits_one — status: draft → code 1; assert "must be 'approved'" in capsys stderr (D-19)
    4. test_one_draft_plan_fails_all — two plans, one draft → code 1 (PLAN-04 belt-and-suspenders for multi-plan)
    5. test_missing_verify_subbullet_exits_one — task line present but no Verify sub-bullet → code 1 (PLAN-02/D-11)
    6. test_no_tasks_exits_one — approved plan with ## Tasks but no [STATUS] TASKID lines → code 1
    7. test_malformed_frontmatter_exits_one — body only, no --- block → code 1
    8. test_missing_phase_dir_exits_one — --phase-dir points to nonexistent path → code 1
    9. test_no_plan_files_exits_one — empty phase dir, no *-PLAN.md → code 1
    10. test_phase_dir_outside_cwd_exits_one — --phase-dir resolves outside tmp_path cwd → code 1 (path containment per RESEARCH Security Domain)

    Lazy import inside each test: `from validate_plan import main` before calling main.

    Commit message: test(04-01): add failing tests for tool-validate-plan exit-code contract
  </action>

  <acceptance_criteria>
    - mise-en-place/tool-validate-plan/test_validate_plan.py exists
    - File contains at least 8 functions named test_* (grep def test_)
    - File contains _plan_body helper with [PENDING] and Verify: patterns
    - File contains sys.path.insert and monkeypatch.chdir
    - File contains test_missing_verify_subbullet_exits_one and test_draft_plan_exits_one
    - python3 -m pytest mise-en-place/tool-validate-plan/test_validate_plan.py --co -q collects all tests
    - Running tests exits non-zero (RED — validate_plan.py missing)
  </acceptance_criteria>

  <verify>
    <automated>grep -cE '^def test_' mise-en-place/tool-validate-plan/test_validate_plan.py && python3 -m pytest mise-en-place/tool-validate-plan/test_validate_plan.py -x -q 2>&1; test $? -ne 0</automated>
  </verify>

  <done>test_validate_plan.py exists with 10 test functions; pytest --co collects all; running tests exits non-zero (RED confirmed); committed with message test(04-01): add failing tests for tool-validate-plan exit-code contract</done>
</task>

<task type="tdd" phase="green">
  <name>GREEN: Write validate_plan.py — make all tests pass</name>
  <files>mise-en-place/tool-validate-plan/validate_plan.py</files>

  <read_first>
    - mise-en-place/tool-validate-plan/test_validate_plan.py (exact expectations each test asserts)
    - mise-en-place/tool-validate-spec/validate_spec.py (parse_frontmatter, status gate, exit-code patterns — copy parse_frontmatter verbatim)
    - mise-en-place/tool-planning-scaffold/planning_scaffold.py (_validate_root path containment logic lines 73-79)
    - .planning/phases/04-plan-phase/04-CONTEXT.md (D-11 validation scope — presence only)
  </read_first>

  <action>
    Write mise-en-place/tool-validate-plan/validate_plan.py with:

    Module docstring documenting: tool-validate-plan purpose, exit 0/1 contract, todo format requirements per D-06/D-09/D-11, --phase-dir CLI.

    Imports: argparse, pathlib, re, sys (stdlib only).

    Constants:
    - TASK_LINE = re.compile(r"^\[(PENDING|IN_PROGRESS|DONE)\]\s+(\S+)\s+-\s+(.+)$")

    Functions:
    - parse_frontmatter(text) -> dict — copy verbatim from validate_spec.py (D-05 reuse, do not reimplement)
    - _validate_phase_dir(raw: str) -> pathlib.Path — resolve; compare to cwd.resolve(); exit 1 with ERROR if outside cwd
    - _find_plan_files(phase_dir: pathlib.Path) -> list[pathlib.Path] — sorted glob **/*-PLAN.md; exit 1 if empty
    - _task_has_verify(body_lines: list[str], task_index: int) -> bool — scan lines after task until next TASK_LINE match or line.startswith("## "); return True if any line contains "Verify:"
    - _validate_plan_file(path: pathlib.Path) -> None — read text; parse frontmatter; if empty dict exit 1 malformed; if status != "approved" exit 1 with path in message; find all task line indices; if none exit 1 no tasks; for each task index call _task_has_verify, exit 1 if missing
    - main(argv=None) -> None — argparse with required --phase-dir; validate path; find plans; validate each; print success count to stdout; sys.exit(0)

    if __name__ == "__main__": main()

    Commit message: feat(04-01): implement validate_plan.py — PLAN.md approval + todo gate
  </action>

  <acceptance_criteria>
    - mise-en-place/tool-validate-plan/validate_plan.py exists
    - File contains def parse_frontmatter( and def main(
    - File contains TASK_LINE or equivalent task regex
    - File contains "Verify:" in validation logic
    - File contains sys.exit(0) and sys.exit(1)
    - File contains --phase-dir argparse argument
    - File contains no non-stdlib imports
    - python3 -m pytest mise-en-place/tool-validate-plan/ -x -q exits 0
    - python3 -m pytest mise-en-place/ -x -q exits 0 (Phase 1–3 tests remain green)
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest mise-en-place/tool-validate-plan/ -x -q && python3 -m pytest mise-en-place/ -x -q</automated>
  </verify>

  <done>All 10 tests pass (GREEN); validate_plan.py committed; full mise-en-place suite green</done>
</task>

<task type="auto">
  <name>Write tool-validate-plan/SKILL.md — agent invocation contract</name>
  <files>mise-en-place/tool-validate-plan/SKILL.md</files>

  <read_first>
    - mise-en-place/tool-validate-spec/SKILL.md (exact structure: frontmatter, cursor_skill_adapter, objective, process with exit code table)
    - mise-en-place/tool-validate-plan/validate_plan.py (CLI args, exit messages, validation scope)
    - .planning/phases/04-plan-phase/04-CONTEXT.md (D-11 presence-only scope; D-19 when to invoke)
  </read_first>

  <action>
    Create mise-en-place/tool-validate-plan/SKILL.md following tool-validate-spec/SKILL.md structure:

    Frontmatter: name: tool-validate-plan, description: "Check that all PLAN.md files in a phase directory are approved and contain valid todo tasks with Verify sub-bullets"

    cursor_skill_adapter: identical A–D sections to tool-validate-spec/SKILL.md.

    objective: Verify all *-PLAN.md files under --phase-dir have status: approved, ≥1 todo task matching [STATUS] TASKID - Title pattern, and each task has a Verify: sub-bullet. Exits 0 on success, 1 with actionable stderr.

    process block:
    - Invocation: python3 mise-en-place/tool-validate-plan/validate_plan.py --phase-dir .planning/phases/{slug}
    - Exit 0 contract: all plans approved, valid todo format
    - Exit 1 failure cases: phase dir missing/outside cwd, no plan files, draft status, malformed frontmatter, no tasks, task missing Verify
    - Document todo format per D-06/D-07/D-08/D-09 with example task line and Verify sub-bullet
    - Explicit scope boundary (D-11): does NOT validate Mermaid graphs, task atomicity/sizing, or wave assignment — those are SKILL-soft enforcement
    - When to invoke: plan-phase after approve-all StrReplace (D-19 confirmation); exec-phase before dispatch (Phase 5 handoff)
    - Example success: ✓ 2 plan(s) approved in .planning/phases/04-plan-phase/
    - Cross-reference: plan-phase writes draft plans; approval gate flips to approved

    Commit message: feat(04-01): add tool-validate-plan/SKILL.md — invocation contract
  </action>

  <acceptance_criteria>
    - mise-en-place/tool-validate-plan/SKILL.md exists
    - File contains name: tool-validate-plan
    - File contains cursor_skill_adapter
    - File contains python3 mise-en-place/tool-validate-plan/validate_plan.py
    - File contains --phase-dir
    - File contains "Exit 0" and "Exit 1"
    - File contains Verify: (todo verification sub-bullet documented)
    - File contains [PENDING] or TASKID pattern example
    - File documents that Mermaid/atomicity are NOT validated (D-11 scope boundary)
    - python3 -m pytest mise-en-place/ -x -q still exits 0
  </acceptance_criteria>

  <verify>
    <automated>grep -q "cursor_skill_adapter" mise-en-place/tool-validate-plan/SKILL.md && grep -q "validate_plan.py" mise-en-place/tool-validate-plan/SKILL.md && grep -q "Verify:" mise-en-place/tool-validate-plan/SKILL.md && python3 -m pytest mise-en-place/ -x -q</automated>
  </verify>

  <done>tool-validate-plan/SKILL.md committed; grep confirms invocation contract and D-11 scope boundary documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → validate_plan.py | Script reads PLAN.md files from --phase-dir; content is developer-controlled local files |
| CLI → validate_plan.py | --phase-dir argument could target paths outside project root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | --phase-dir path traversal | mitigate | Resolve path; reject if outside cwd (mirror planning_scaffold._validate_root) |
| T-04-02 | Denial of Service | TASK_LINE regex on large files | mitigate | Linear regex with anchored pattern; no nested quantifiers (RESEARCH ReDoS guidance) |
| T-04-03 | Tampering | Manual status: approved edit bypass | accept | Developer owns local files; tool enforces workflow discipline not access control |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — stdlib only |

</threat_model>

<verification>
After all three tasks complete:

```shell
python3 -m pytest mise-en-place/tool-validate-plan/ -x -q
python3 -m pytest mise-en-place/ -x -q
```

Both must exit 0.
</verification>

<success_criteria>
- python3 -m pytest mise-en-place/tool-validate-plan/ -x -q exits 0 (10/10 tests pass)
- validate_plan.py enforces status: approved, todo pattern, and Verify presence per D-11
- validate_plan.py rejects --phase-dir outside cwd
- tool-validate-plan/SKILL.md documents invocation and D-11 scope boundary
- Phase 1–3 tests remain green
- Three atomic commits: RED tests, GREEN implementation, SKILL contract
</success_criteria>

<output>
Create .planning/phases/04-plan-phase/04-01-SUMMARY.md when done
</output>
