---
phase: 03-brownfield-codebase-onboarding
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - mise-en-place/map-codebase/SKILL.md
  - mise-en-place/spec-phase/SKILL.md
  - mise-en-place/README.md
autonomous: true
requirements:
  - PROJ-02
  - PROJ-03
must_haves:
  truths:
    - "Developer can invoke map-codebase workflow and receive seven .planning/codebase/*.md files"
    - "Running /spec-phase on a brownfield repo triggers detect_brownfield after Step 1 scaffold, before Q&A"
    - "When needs_codebase_map is true (including empty scaffolded .planning/codebase/), spec-phase runs map-codebase workflow before Q&A"
    - "Brownfield Q&A pre-loads STACK.md, ARCHITECTURE.md, STRUCTURE.md summaries and skips re-asking mapped stack/structure facts"
    - "Brownfield SPEC.md assembly includes ## Already Built and ## To Build with project_type: brownfield in frontmatter"
    - "Greenfield repos skip Step 0 mapping and produce standard five-section SPEC.md unchanged"
  artifacts:
    - path: "mise-en-place/map-codebase/SKILL.md"
      provides: "Parallel mapper workflow writing seven codebase map files"
      contains: "STACK.md"
    - path: "mise-en-place/spec-phase/SKILL.md"
      provides: "Step 0 brownfield branch + brownfield Q&A + assembly rules"
      contains: "Step 1.4"
  key_links:
    - from: "spec-phase/SKILL.md Step 1.4"
      to: "detect_brownfield.py"
      via: "Shell: python3 mise-en-place/tool-detect-brownfield/detect_brownfield.py"
      pattern: "tool-detect-brownfield"
    - from: "spec-phase/SKILL.md Step 1.4"
      to: "map-codebase/SKILL.md"
      via: "needs_codebase_map true → read and execute map-codebase workflow"
      pattern: "map-codebase"
    - from: "spec-phase/SKILL.md Step 4"
      to: "validate_spec.py BROWNFIELD_SECTIONS"
      via: "assembly headers ## Already Built and ## To Build match validator"
      pattern: "## Already Built"
    - from: "map-codebase/SKILL.md"
      to: ".planning/codebase/*.md"
      via: "Task generalPurpose sub-agents Write tool"
      pattern: ".planning/codebase/"
---

## Phase Goal

**As a** developer, **I want to** run `/spec-phase` on an existing codebase and get a spec that separates what's already built from what's new, **so that** I don't re-plan or re-ask about capabilities that already exist.

<objective>
Deliver the brownfield agent workflow vertical slice: `map-codebase/SKILL.md` (standalone + spec-phase-integrated) and extended `spec-phase/SKILL.md` with Step 0 detection → mapping → delta-focused Q&A → SPEC assembly with Already Built / To Build sections.

Purpose: PROJ-02 requires codebase mapping before spec Q&A when code exists; PROJ-03 requires the map to seed spec context and enforce separation in SPEC.md. Depends on Plan 03-01 detection and validate_spec contracts.

Output: `map-codebase/SKILL.md`, extended `spec-phase/SKILL.md`, updated `README.md`.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-brownfield-codebase-onboarding/03-RESEARCH.md
@.planning/phases/03-brownfield-codebase-onboarding/03-PATTERNS.md
@.planning/phases/02-spec-phase-greenfield/02-CONTEXT.md
@.planning/phases/03-brownfield-codebase-onboarding/03-01-SUMMARY.md

<interfaces>
From Plan 03-01 (must exist before this plan runs):
- detect_brownfield.py JSON: is_brownfield, needs_codebase_map, has_codebase_map, code_extensions_found
- validate_spec.py BROWNFIELD_SECTIONS: "## Already Built", "## To Build" (when project_type: brownfield)

From mise-en-place/spec-phase/SKILL.md (extend, preserve Steps 1–5 greenfield flow):
- Step 1: planning_scaffold.py scaffold + mkdir .planning/research
- Step 2: five-category Q&A spine (Problem → Who it's for → Constraints → Success Criteria → Out-of-scope)
- Step 4: REQUIRED_SECTIONS headers must match validate_spec.py exactly
- Sub-agent convention: Task(subagent_type="generalPurpose"), no model parameter

From .cursor/skills/gsd-map-codebase/SKILL.md + .cursor/get-shit-done/workflows/map-codebase.md:
- 4 parallel mapper focuses: tech→STACK+INTEGRATIONS, arch→ARCHITECTURE+STRUCTURE, quality→CONVENTIONS+TESTING, concerns→CONCERNS
- check_existing prompt: Refresh / Update / Skip when .planning/codebase/ exists
- Template reference: .cursor/get-shit-done/templates/codebase/*.md

Seven canonical map files: STACK.md, ARCHITECTURE.md, CONVENTIONS.md, STRUCTURE.md, TESTING.md, INTEGRATIONS.md, CONCERNS.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Slice 1: map-codebase SKILL — standalone parallel mapper workflow</name>
  <files>mise-en-place/map-codebase/SKILL.md</files>

  <read_first>
    - .cursor/skills/gsd-map-codebase/SKILL.md (orchestration, 4-agent assignment, check_existing)
    - .cursor/get-shit-done/workflows/map-codebase.md (refresh/skip/update flow)
    - mise-en-place/spec-phase/SKILL.md (sub-agent spawning convention lines 24-27, research sub-agent prompt pattern)
    - .cursor/get-shit-done/templates/codebase/*.md (section structure for mapper prompts)
    - .planning/codebase/STACK.md (live output format reference)
    - mise-en-place/tool-context-builder/SKILL.md (context_builder invocation to seed mappers)
  </read_first>

  <action>
    Create mise-en-place/map-codebase/SKILL.md as a workflow skill (no Python script):

    Frontmatter: name map-codebase, description for analyzing existing codebase into seven structured documents.

    Copy cursor_skill_adapter block from spec-phase/SKILL.md (use generalPurpose sub-agents, no model param).

    Objective: parallel mapper sub-agents write directly to .planning/codebase/; orchestrator receives confirmations only (not full document contents).

    Process steps:
    1. Run planning_scaffold.py scaffold to ensure .planning/codebase/ exists
    2. check_existing: if .planning/codebase/ has files, present numbered options — (1) Refresh delete and remap (2) Update specific docs (3) Skip use existing — per GSD map-codebase workflow
    3. Optionally run context_builder.py and append stdout to sub-agent prompts as raw snapshot
    4. Spawn 4 parallel Task(subagent_type="generalPurpose") mappers:
       - tech focus → writes STACK.md, INTEGRATIONS.md
       - arch focus → writes ARCHITECTURE.md, STRUCTURE.md
       - quality focus → writes CONVENTIONS.md, TESTING.md
       - concerns focus → writes CONCERNS.md
    5. Each sub-agent prompt MUST include: focus area, output file paths, template sections from .cursor/get-shit-done/templates/codebase/, rules (concrete paths with backticks, current state only, mark uncertain [unverified], confirm paths + line counts in final message only — do NOT return full doc contents)
    6. Security rule per RESEARCH: note .env existence only, never read secret contents
    7. Verify all 7 files exist after agents complete; report line counts

    Optional --fast mode (research discretion): document single-agent writes-all-7 for repos with fewer than 5 source files; default remains 4-agent parallel.

    Skill invokable standalone via /map-codebase OR map-codebase mention — not only from spec-phase.
  </action>

  <acceptance_criteria>
    - test -f mise-en-place/map-codebase/SKILL.md
    - grep -c "STACK.md" mise-en-place/map-codebase/SKILL.md returns >= 1
    - grep -c "generalPurpose" mise-en-place/map-codebase/SKILL.md returns >= 1
    - grep -c "Refresh" mise-en-place/map-codebase/SKILL.md returns >= 1
    - grep -c ".planning/codebase" mise-en-place/map-codebase/SKILL.md returns >= 3
    - SKILL.md documents all seven output filenames explicitly
  </acceptance_criteria>

  <verify>
    <automated>grep -v '^#' mise-en-place/map-codebase/SKILL.md | grep -cE 'STACK\.md|ARCHITECTURE\.md|CONVENTIONS\.md|STRUCTURE\.md|TESTING\.md|INTEGRATIONS\.md|CONCERNS\.md' | test "$(grep -v '^#' mise-en-place/map-codebase/SKILL.md | grep -cE 'STACK\.md|ARCHITECTURE\.md|CONVENTIONS\.md|STRUCTURE\.md|TESTING\.md|INTEGRATIONS\.md|CONCERNS\.md')" -ge 7</automated>
  </verify>

  <done>map-codebase/SKILL.md defines complete standalone mapper workflow with 4-focus parallel agents and seven-file output contract</done>
</task>

<task type="auto">
  <name>Slice 2: spec-phase Step 1.4 — detect brownfield and trigger mapping before Q&A</name>
  <files>mise-en-place/spec-phase/SKILL.md</files>

  <read_first>
    - mise-en-place/spec-phase/SKILL.md (full file — insert Step 0 without breaking Steps 1-5)
    - mise-en-place/tool-detect-brownfield/SKILL.md (from Plan 03-01)
    - mise-en-place/map-codebase/SKILL.md (from prior task)
    - .planning/phases/03-brownfield-codebase-onboarding/03-RESEARCH.md (Step 0 brownfield branch, Pattern 4)
  </read_first>

  <action>
    Extend spec-phase/SKILL.md per RESEARCH Pattern 4 and user_constraints (extend Phase 2, do not replace):

    1. Update objective opening line from "empty repo" to "empty or existing repo" — brownfield supported.

    2. Add **Step 1.4: Brownfield Detection** as a sub-step at the END of existing Step 1 (after scaffold + mkdir .planning/research). Do NOT insert anything before Step 1 — detection runs only after scaffold creates paths:
       a. Run: python3 mise-en-place/tool-detect-brownfield/detect_brownfield.py
       b. Parse JSON from stdout
       c. If is_brownfield false → skip to Step 2 greenfield Q&A (no mapping)
       d. If is_brownfield true:
          - If needs_codebase_map true → execute map-codebase workflow (read map-codebase/SKILL.md). This covers first-time brownfield runs where scaffold created an empty .planning/codebase/ dir — detect_brownfield treats empty dir as needs_codebase_map true (STACK.md absent)
          - If has_codebase_map true AND needs_codebase_map false → offer check_existing Refresh/Update/Skip per map-codebase SKILL (only when STACK.md and other canonical files exist)
          - Defense-in-depth: if .planning/codebase/STACK.md is missing after detection, always execute map-codebase regardless of has_codebase_map flag
          - Read .planning/codebase/STACK.md, ARCHITECTURE.md, STRUCTURE.md (summaries only — do NOT dump all seven files into prompt)
          - Present 3-5 bullet summary of detected capabilities; ask user to confirm/correct before Q&A (human confirmation gate per RESEARCH Pitfall 3)
          - Set internal brownfield mode flag for Step 2

    3. Document that Step 1.4 preserves clean-context constraint (SPEC-05): all inputs from local files and tool stdout only.

    Label in SKILL.md: "Step 1.4: Brownfield Detection (runs after scaffold)" — no contradictory "before Step 1" language.
  </action>

  <acceptance_criteria>
    - grep -c "Step 1.4" mise-en-place/spec-phase/SKILL.md returns >= 1 OR grep -c "Brownfield Detection" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "STACK.md" mise-en-place/spec-phase/SKILL.md returns >= 1 (canonical map marker referenced)
    - grep -c "empty" mise-en-place/spec-phase/SKILL.md returns >= 1 OR grep -c "scaffold" mise-en-place/spec-phase/SKILL.md returns >= 2 (empty scaffold case documented)
    - grep -c "tool-detect-brownfield" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "map-codebase" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "needs_codebase_map" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "is_brownfield" mise-en-place/spec-phase/SKILL.md returns >= 1
    - Greenfield path documented: is_brownfield false skips to Step 2 without mapping
  </acceptance_criteria>

  <verify>
    <automated>grep -v '^#' mise-en-place/spec-phase/SKILL.md | grep -cE 'Step 1\.4|Brownfield Detection|tool-detect-brownfield|needs_codebase_map|STACK\.md' | test "$(grep -v '^#' mise-en-place/spec-phase/SKILL.md | grep -cE 'Step 1\.4|Brownfield Detection|tool-detect-brownfield|needs_codebase_map|STACK\.md')" -ge 4</automated>
  </verify>

  <done>spec-phase triggers detection and conditional mapping before Q&A on brownfield repos; greenfield path unchanged</done>
</task>

<task type="auto">
  <name>Slice 3: Brownfield Q&A, SPEC assembly, and README catalog</name>
  <files>mise-en-place/spec-phase/SKILL.md, mise-en-place/README.md</files>

  <read_first>
    - mise-en-place/spec-phase/SKILL.md (Step 2 Q&A and Step 4 assembly sections)
    - mise-en-place/tool-validate-spec/validate_spec.py (REQUIRED_SECTIONS + BROWNFIELD_SECTIONS from Plan 03-01)
    - .planning/phases/03-brownfield-codebase-onboarding/03-RESEARCH.md (Pattern 3 SPEC format, confidence tags, Q&A delta focus)
    - mise-en-place/README.md
  </read_first>

  <action>
    Extend spec-phase/SKILL.md Step 2 for brownfield mode (when Step 0 set brownfield flag):
    - Same five-category spine order preserved (Phase 2 D-01)
    - Do NOT re-ask stack, directory structure, or capabilities already documented in codebase map
    - Problem and Success Criteria focus on DELTA — new work only, not inventory of existing code
    - Success Criteria must reference To Build items only in brownfield mode (no mixing built capabilities)
    - Confidence tags on Already Built items during Q&A confirmation: [observed] for direct code evidence, [inferred] for pattern-based, [unverified] for uncertain (research discretion)

    Extend spec-phase/SKILL.md Step 4 assembly for brownfield:
    - Frontmatter adds project_type: brownfield (or greenfield when not brownfield — default greenfield)
    - Optional codebase_map_commit: git rev-parse HEAD when git available (skip gracefully if not repo)
    - Body order for brownfield: ## Already Built (from map + user confirmation), ## To Build (active requirements), then existing five REQUIRED_SECTIONS
    - Assembly rule: every To Build item must be a gap not covered in Already Built — no duplication across sections (RESEARCH Pitfall 3)
    - Header strings must match validate_spec.py exactly including ## Already Built and ## To Build

    Step 5 approval gate unchanged — validate_spec handles brownfield sections when approved.

    Update mise-en-place/README.md:
    - Add ### map-codebase section describing seven-file output and standalone invocation
    - Update ### spec-phase description to mention brownfield detection, mapping, and Already Built / To Build sections
    - Cross-reference tool-detect-brownfield from Plan 03-01
  </action>

  <acceptance_criteria>
    - grep -c "## Already Built" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "## To Build" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "project_type" mise-en-place/spec-phase/SKILL.md returns >= 1
    - grep -c "map-codebase" mise-en-place/README.md returns >= 1
    - grep -c "brownfield" mise-en-place/README.md returns >= 1
    - grep -c "delta" mise-en-place/spec-phase/SKILL.md returns >= 1 OR grep -c "DELTA" mise-en-place/spec-phase/SKILL.md returns >= 1
    - python3 -m pytest mise-en-place/ -q exits 0 (no test regressions from doc-only changes)
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest mise-en-place/ -q && grep -v '^#' mise-en-place/spec-phase/SKILL.md | grep -cE 'Already Built|To Build|project_type' | test "$(grep -v '^#' mise-en-place/spec-phase/SKILL.md | grep -cE 'Already Built|To Build|project_type')" -ge 3</automated>
  </verify>

  <done>Brownfield Q&A skips mapped facts; SPEC assembly produces separated Already Built / To Build sections; README documents full brownfield workflow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mapper sub-agents → filesystem | Agents read project source and write .planning/codebase/ |
| spec-phase → user Q&A | Conversational flow loads map summaries into agent context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | map-codebase mapper agents | mitigate | SKILL.md rule: note .env existence only, never read secret file contents |
| T-03-05 | Tampering | Stale codebase map | accept | MVP: offer Refresh/Skip on existing map; auto-drift deferred to Phase 6 |
| T-03-06 | Spoofing | Inferred capabilities as requirements | mitigate | Human confirmation gate after map before Q&A; [inferred]/[unverified] tags |
| T-03-SC | Tampering | npm/pip installs | accept | No package installs in this plan — SKILL.md workflow only |

</threat_model>

<verification>
Automated: full pytest suite green after README edits.

Manual UAT per 03-VALIDATION.md (not blocking plan completion but required before phase verify):
- Run /map-codebase on this repo → 7 files in .planning/codebase/
- Run /spec-phase on this repo → detect → map → Q&A references map, focuses on delta
- Complete brownfield spec → SPEC.md has distinct Already Built and To Build sections
- Run /spec-phase on empty tmp dir → no mapping step, standard five sections
</verification>

<success_criteria>
- map-codebase/SKILL.md invokable standalone with 4-focus parallel mapper contract
- spec-phase Step 1.4 integrates detect_brownfield + conditional map-codebase
- Brownfield SPEC.md assembly matches validate_spec BROWNFIELD_SECTIONS contract
- Greenfield flow preserved for is_brownfield false repos
- README documents map-codebase and brownfield spec-phase
</success_criteria>

<output>
Create `.planning/phases/03-brownfield-codebase-onboarding/03-02-SUMMARY.md` when done
</output>
