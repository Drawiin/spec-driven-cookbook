---
phase: 03-brownfield-codebase-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mise-en-place/tool-detect-brownfield/detect_brownfield.py
  - mise-en-place/tool-detect-brownfield/test_detect_brownfield.py
  - mise-en-place/tool-detect-brownfield/SKILL.md
  - mise-en-place/tool-validate-spec/validate_spec.py
  - mise-en-place/tool-validate-spec/test_validate_spec.py
  - mise-en-place/README.md
autonomous: true
requirements:
  - PROJ-02
  - PROJ-03
must_haves:
  truths:
    - "Running detect_brownfield.py on a repo with source files prints JSON with is_brownfield: true and exits 0"
    - "Running detect_brownfield.py on an empty directory prints JSON with is_brownfield: false and exits 0"
    - "needs_codebase_map is true when brownfield and canonical map files are absent (empty scaffolded .planning/codebase/ counts as absent)"
    - "has_codebase_map is true and needs_codebase_map is false when STACK.md exists at .planning/codebase/STACK.md"
    - "validate_spec.py rejects brownfield SPEC.md missing ## Already Built or ## To Build when project_type: brownfield"
    - "validate_spec.py rejects SPEC.md containing ## Already Built or ## To Build when project_type is absent or not brownfield (inverse rule — PROJ-03 structural guarantee)"
    - "validate_spec.py rejects brownfield SPEC.md missing any greenfield REQUIRED_SECTION (e.g. ## Problem)"
    - "validate_spec.py still passes greenfield approved specs without brownfield sections (22+ existing tests stay green)"
  artifacts:
    - path: "mise-en-place/tool-detect-brownfield/detect_brownfield.py"
      provides: "Stdlib brownfield detection with JSON stdout contract"
      contains: "is_brownfield"
    - path: "mise-en-place/tool-detect-brownfield/test_detect_brownfield.py"
      provides: "pytest coverage for detection contract"
      contains: "test_detect_brownfield_with_py_files"
    - path: "mise-en-place/tool-detect-brownfield/SKILL.md"
      provides: "Agent invocation contract for detection tool"
    - path: "mise-en-place/tool-validate-spec/validate_spec.py"
      provides: "Brownfield section gate + inverse rule when brownfield sections present"
      contains: "BROWNFIELD_SECTIONS"
  key_links:
    - from: "detect_brownfield.py:main()"
      to: "GSD init.cjs detection heuristic"
      via: "depth-3 walk with CODE_EXTENSIONS and SKIP_DIRS; follow_symlinks=False"
      pattern: "CODE_EXTENSIONS"
    - from: "detect_brownfield.py:main()"
      to: "context_builder path validation"
      via: "root.relative_to(cwd) pattern — reject paths outside cwd"
      pattern: "relative_to"
    - from: "validate_spec.py:main()"
      to: "SPEC.md project_type frontmatter"
      via: "project_type normalized with strip().lower()"
      pattern: "project_type"
    - from: "validate_spec.py:main()"
      to: "brownfield body sections"
      via: "inverse rule: brownfield sections in body require project_type brownfield; forward rule: brownfield project_type requires BROWNFIELD_SECTIONS"
      pattern: "BROWNFIELD_SECTIONS"
---

## Phase Goal

**As a** developer, **I want to** run deterministic detection and validation on an existing codebase, **so that** the framework knows when mapping is required and brownfield specs enforce Already Built vs To Build separation before planning.

<objective>
Deliver the deterministic brownfield foundation: `tool-detect-brownfield` (stdlib JSON detection) and `validate_spec.py` brownfield section gates — with full pytest coverage via TDD RED→GREEN.

Purpose: PROJ-02 requires repeatable detection before spec Q&A; PROJ-03 requires validate_spec to enforce `## Already Built` and `## To Build` when `project_type: brownfield`, and to reject brownfield sections without matching frontmatter (inverse rule). Plan 02 (agent workflows) depends on these contracts.

Output: `detect_brownfield.py`, `test_detect_brownfield.py`, `tool-detect-brownfield/SKILL.md`, extended `validate_spec.py` + `test_validate_spec.py`, README catalog entry.
</objective>

<execution_context>
@.cursor/get-shit-done/workflows/execute-plan.md
@.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-brownfield-codebase-onboarding/03-RESEARCH.md
@.planning/phases/03-brownfield-codebase-onboarding/03-PATTERNS.md
@.planning/phases/03-brownfield-codebase-onboarding/03-VALIDATION.md

<interfaces>
From mise-en-place/tool-validate-spec/validate_spec.py (extend, do not break):
- REQUIRED_SECTIONS: five greenfield headers (## Problem, ## Who It's For, ## Constraints, ## Success Criteria, ## Out of Scope)
- parse_frontmatter(text: str) -> dict — reuse unchanged
- main() exit 0 only when status: approved + all required sections present

From mise-en-place/tool-context-builder/context_builder.py (path validation pattern, lines 118-127):
- --root must equal cwd or be descendant of cwd; exit 1 with ERROR: prefix on stderr if invalid
- Prefer root.relative_to(cwd) pattern over string prefix checks (security.cjs validatePath alignment)

From .cursor/get-shit-done/bin/lib/init.cjs (port detection algorithm):
- CODE_EXTENSIONS set, SKIP_DIRS set, PACKAGE_FILES tuple, max_depth=3 recursive walk
- is_brownfield = has_existing_code OR has_package_file
- has_codebase_map = canonical map file STACK.md exists at .planning/codebase/STACK.md (empty scaffolded dir alone is NOT a map)
- needs_codebase_map = is_brownfield AND NOT has_codebase_map

JSON stdout schema (detection success always exit 0):
{ "is_brownfield", "has_existing_code", "has_package_file", "has_codebase_map", "needs_codebase_map", "code_extensions_found" }

validate_spec inverse rule (PROJ-03):
- If body contains "## Already Built" OR "## To Build" → require project_type (after strip().lower()) == "brownfield"
- project_type parsing: fm.get("project_type", "greenfield").strip().lower()
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>RED: Write failing tests for detect_brownfield and brownfield validate_spec</name>
  <files>mise-en-place/tool-detect-brownfield/test_detect_brownfield.py, mise-en-place/tool-validate-spec/test_validate_spec.py</files>

  <read_first>
    - mise-en-place/tool-context-builder/test_context_builder.py (subprocess + tmp_path fixture pattern)
    - mise-en-place/tool-validate-spec/test_validate_spec.py (CANONICAL_SECTIONS helper, monkeypatch.chdir, SystemExit assertions)
    - .planning/phases/03-brownfield-codebase-onboarding/03-VALIDATION.md (test function names for 03-01-01 through 03-01-11)
    - .planning/phases/03-brownfield-codebase-onboarding/03-PATTERNS.md (detect_brownfield test patterns)
  </read_first>

  <behavior>
    detect_brownfield tests:
    - test_detect_brownfield_with_py_files: tmp dir with src/main.py → subprocess run detect_brownfield.py → exit 0, JSON is_brownfield true
    - test_detect_greenfield_empty: empty tmp dir → exit 0, JSON is_brownfield false
    - test_needs_codebase_map: brownfield tmp without .planning/codebase/ → needs_codebase_map true
    - test_needs_codebase_map_empty_scaffold: brownfield tmp with empty .planning/codebase/ dir (scaffold-only) → needs_codebase_map true, has_codebase_map false
    - test_has_codebase_map_when_stack_md_present: brownfield tmp with .planning/codebase/STACK.md (non-empty) → has_codebase_map true, needs_codebase_map false
    - test_brownfield_package_file_only: tmp with package.json only (no .py or other code files) → is_brownfield true, has_package_file true
    - test_reject_root_outside_cwd: --root pointing outside cwd → exit 1, stderr contains ERROR

    validate_spec brownfield tests (extend test_validate_spec.py):
    - test_brownfield_missing_already_built: project_type brownfield, approved, missing ## Already Built → exit 1, stderr mentions Already Built
    - test_brownfield_missing_to_build: missing ## To Build → exit 1
    - test_brownfield_approved_passes: all brownfield + greenfield sections → exit 0
    - test_brownfield_sections_without_project_type_fails: body contains ## Already Built and ## To Build but frontmatter omits project_type → exit 1, stderr mentions project_type or brownfield
    - test_brownfield_missing_problem_section: project_type brownfield with brownfield sections but missing ## Problem → exit 1, stderr mentions Problem
    - test_greenfield_regression_still_passes: existing test_approved_spec_exits_zero unchanged behavior (no brownfield sections required)

    All new tests MUST fail before implementation exists (ImportError or assertion failure).
  </behavior>

  <action>
    Create mise-en-place/tool-detect-brownfield/ directory. Write test_detect_brownfield.py using sys.path.insert, tmp_path fixtures, subprocess.run against detect_brownfield.py (script path may not exist yet — tests import or subprocess will fail RED).

    Extend test_validate_spec.py with helper _brownfield_spec_body(include_brownfield=True, project_type="brownfield") that adds frontmatter project_type and optional ## Already Built / ## To Build sections before CANONICAL_SECTIONS. Add helper variant omitting project_type for inverse-rule test. Add the six brownfield test functions listed in behavior block.

    Do NOT implement detect_brownfield.py or modify validate_spec.py in this task — tests only.
  </action>

  <acceptance_criteria>
    - python3 -m pytest mise-en-place/tool-detect-brownfield/test_detect_brownfield.py -x -q exits non-zero (RED — no implementation)
    - python3 -m pytest mise-en-place/tool-validate-spec/test_validate_spec.py::test_brownfield_missing_already_built -x -q exits non-zero (RED)
    - test_detect_brownfield.py contains test_detect_brownfield_with_py_files, test_detect_greenfield_empty, test_needs_codebase_map, test_has_codebase_map_when_stack_md_present, test_brownfield_package_file_only, test_reject_root_outside_cwd
    - test_validate_spec.py contains test_brownfield_sections_without_project_type_fails, test_brownfield_missing_problem_section
    - Existing six greenfield validate_spec tests still collect (may fail on new brownfield tests only, not on existing six)
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest mise-en-place/tool-detect-brownfield/test_detect_brownfield.py mise-en-place/tool-validate-spec/test_validate_spec.py -x -q 2>&1; test $? -ne 0</automated>
  </verify>

  <done>All new test functions exist and fail RED; no production implementation files created yet</done>
</task>

<task type="tdd" tdd="true">
  <name>GREEN: Implement detect_brownfield.py and extend validate_spec.py for brownfield sections</name>
  <files>mise-en-place/tool-detect-brownfield/detect_brownfield.py, mise-en-place/tool-validate-spec/validate_spec.py</files>

  <read_first>
    - mise-en-place/tool-context-builder/context_builder.py (--root validation, SKIP_DIRS pattern)
    - mise-en-place/tool-validate-spec/validate_spec.py (current main() flow — extend after greenfield checks)
    - .cursor/get-shit-done/bin/lib/security.cjs (validatePath — root.relative_to(cwd) pattern for path containment)
    - .planning/phases/03-brownfield-codebase-onboarding/03-RESEARCH.md (Pattern 1 JSON schema, detection constants)
    - mise-en-place/tool-detect-brownfield/test_detect_brownfield.py (tests from prior task)
  </read_first>

  <action>
    Implement detect_brownfield.py per RESEARCH Pattern 1 and PATTERNS.md:
    - Shebang, module docstring describing JSON stdout contract
    - Imports: argparse, json, pathlib, sys only (stdlib per locked decision)
    - Constants CODE_EXTENSIONS, SKIP_DIRS, PACKAGE_FILES from RESEARCH.md
    - find_code_files(root, depth=0, max_depth=3) using os.walk or pathlib with follow_symlinks=False on directory iteration — never follow symlinks out of root
    - has_package_file check for PACKAGE_FILES at root
    - CANONICAL_MAP_MARKER = root / ".planning" / "codebase" / "STACK.md"
    - has_codebase_map = CANONICAL_MAP_MARKER.is_file()  (empty scaffolded dir is NOT a map — fixes scaffold-then-detect gap)
    - --root validation: resolve root and cwd; use root.relative_to(cwd) to confirm containment (ValueError → exit 1 with ERROR: prefix on stderr); reject non-dir
    - Print json.dumps(result, indent=2) to stdout; exit 0 on successful detection; exit 1 only on path errors

    Extend validate_spec.py per RESEARCH Pattern 3 (Pitfall 4: do NOT add to REQUIRED_SECTIONS):
    - Add BROWNFIELD_SECTIONS = ["## Already Built", "## To Build"]
    - Normalize project_type: project_type = fm.get("project_type", "greenfield").strip().lower()
    - Inverse rule (before forward brownfield gate): if any header in BROWNFIELD_SECTIONS appears in text AND project_type != "brownfield" → stderr "ERROR: SPEC.md contains brownfield sections but project_type is not brownfield" exit 1
    - After existing REQUIRED_SECTIONS loop passes, if project_type == "brownfield", loop BROWNFIELD_SECTIONS; missing header → stderr "ERROR: brownfield SPEC.md missing: {header}" exit 1
    - Greenfield default unchanged — omit brownfield sections entirely when project_type absent or greenfield
    - Optionally extend success stdout to mention project_type when brownfield

    Update validate_spec.py module docstring to document brownfield conditional sections and inverse rule.
  </action>

  <acceptance_criteria>
    - python3 -m pytest mise-en-place/tool-detect-brownfield/ mise-en-place/tool-validate-spec/ -x -q exits 0
    - python3 mise-en-place/tool-detect-brownfield/detect_brownfield.py run from repo root exits 0 and stdout JSON contains "is_brownfield": true
    - python3 mise-en-place/tool-validate-spec/validate_spec.py against greenfield approved fixture (no project_type) still exits 0 — regression
    - grep -v '^#' mise-en-place/tool-validate-spec/validate_spec.py | grep -c BROWNFIELD_SECTIONS returns >= 1
    - grep -v '^#' mise-en-place/tool-detect-brownfield/detect_brownfield.py | grep -c follow_symlinks returns >= 1 OR grep -v '^#' mise-en-place/tool-detect-brownfield/detect_brownfield.py | grep -c relative_to returns >= 1
    - No pip imports added — stdlib only
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest mise-en-place/tool-detect-brownfield/ mise-en-place/tool-validate-spec/ -x -q</automated>
  </verify>

  <done>All detect + validate tests GREEN; JSON detection contract, inverse rule, and brownfield section gate implemented</done>
</task>

<task type="auto">
  <name>Document tool-detect-brownfield invocation contract and update README</name>
  <files>mise-en-place/tool-detect-brownfield/SKILL.md, mise-en-place/README.md</files>

  <read_first>
    - mise-en-place/tool-context-builder/SKILL.md (tool SKILL.md frontmatter + cursor_skill_adapter + process block)
    - mise-en-place/tool-validate-spec/SKILL.md (exit code documentation pattern)
    - mise-en-place/tool-detect-brownfield/detect_brownfield.py (actual JSON fields and --root args)
    - mise-en-place/README.md (existing tool entry format)
  </read_first>

  <action>
    Write tool-detect-brownfield/SKILL.md:
    - Frontmatter name: tool-detect-brownfield, description for brownfield/greenfield detection
    - Copy cursor_skill_adapter block verbatim from any existing mise-en-place tool SKILL.md
    - Document invocation: python3 mise-en-place/tool-detect-brownfield/detect_brownfield.py [--root PATH]
    - Document JSON stdout schema with all six fields and their meanings
    - Document exit 0 = successful detection (regardless of brownfield result); exit 1 = invalid --root only
    - Note stderr uses ERROR: prefix for path failures; walk uses follow_symlinks=False

    Update mise-en-place/README.md:
    - Add ### tool-detect-brownfield section after tool-validate-spec describing detection purpose and JSON output
    - Note validate_spec now supports project_type: brownfield with ## Already Built and ## To Build sections, plus inverse rule when brownfield sections present without project_type
    - Update Development section quick-run example to include tool-detect-brownfield path
    - Fix stderr note if README says all errors go to stdout — detect and validate print errors to stderr
  </action>

  <acceptance_criteria>
    - test -f mise-en-place/tool-detect-brownfield/SKILL.md
    - grep -c is_brownfield mise-en-place/tool-detect-brownfield/SKILL.md returns >= 1
    - grep -c tool-detect-brownfield mise-en-place/README.md returns >= 1
    - python3 -m pytest mise-en-place/ -q exits 0 (full suite regression)
  </acceptance_criteria>

  <verify>
    <automated>python3 -m pytest mise-en-place/ -q</automated>
  </verify>

  <done>SKILL.md documents detection contract; README catalog includes tool-detect-brownfield and brownfield validate_spec note</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI --root argument | Untrusted path input crosses into filesystem walk |
| SPEC.md body | Untrusted file content parsed for section headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | detect_brownfield.py --root | mitigate | Resolve path; use root.relative_to(cwd) to reject outside cwd; follow_symlinks=False on walk |
| T-03-02 | Denial of Service | detect_brownfield filesystem walk | mitigate | max_depth=3; SKIP_DIRS excludes node_modules, .git, .planning |
| T-03-03 | Tampering | validate_spec section scan / project_type bypass | mitigate | Inverse rule: brownfield sections in body require project_type brownfield; normalize project_type with strip().lower() |
| T-03-SC | Tampering | Python stdlib scripts | accept | No external package installs in this plan |

</threat_model>

<verification>
Per 03-VALIDATION.md sampling: after each task commit run `python3 -m pytest mise-en-place/tool-detect-brownfield/ mise-en-place/tool-validate-spec/ -x -q`; after plan complete run full `python3 -m pytest mise-en-place/ -q`.
</verification>

<success_criteria>
- detect_brownfield.py returns correct JSON for brownfield and greenfield tmp fixtures including STACK-present and package-only cases
- validate_spec.py enforces brownfield sections when project_type: brownfield and rejects brownfield sections without project_type (inverse rule)
- validate_spec.py rejects brownfield specs missing greenfield REQUIRED_SECTIONS
- All Phase 1–2 tests remain green (22 baseline + new tests)
- tool-detect-brownfield/SKILL.md and README updated
</success_criteria>

<output>
Create `.planning/phases/03-brownfield-codebase-onboarding/03-01-SUMMARY.md` when done
</output>
